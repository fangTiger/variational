# 侦察抓取的前端打包代码（体积大、随前端更新变化，可用 fetch_bundle.py 复现）
tools/recon/captured/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
env/
.env

# 日志（log/ 是历史笔误，实际目录是 logs/，两个都留着防回潮）
log/
logs/

# IDE
.idea/
.vscode/

# 密钥/会话（绝不入库）
secrets/
*.session
cookies.json

# 账户细节：实盘金额、权益、持仓、vault/钱包地址
# README 是公开的，任何带具体金额或账户标识的记录都放这里
docs/private/

# 运行数据快照（整目录忽略：原先按后缀列举会漏掉 .bak-* 等备份）
data/
data/basis_samples_*.jsonl
