## 0. 前置

- [x] 0.1 记录完整 `tests/` 基线通过数（758 passed）

## 1. 失败路径测试（先写，必须先失败）

- [x] 1.1 心跳陈旧（超过 3×interval）→ 触发互锁
- [x] 1.2 心跳文件不存在 → 触发互锁，**不得**当作正常
- [x] 1.3 心跳末行损坏 / 缺时间戳 → 触发互锁
- [x] 1.4 心跳新鲜但自报两腿读取失败 → 触发互锁
- [x] 1.5 互锁生效 → 扩大敞口的挂单被拒
- [x] 1.6 互锁生效 → 缩小敞口的挂单**放行**
- [x] 1.7 互锁生效 → 成交后的闭环翻单**不被阻止**（防 2026-08-10 单向抄底重演）
- [x] 1.8 互锁生效 → 撤单照常执行
- [x] 1.9 互锁生效且库存为零 → 双向开仓均被拒
- [x] 1.10 互锁触发/恢复各告警一次，持续期间限频（断言日志条数）
- [x] 1.11 互锁状态可从做市心跳读到
- [x] 1.12 未配置互锁 → 不读心跳文件，逐单行为与变更前一致
- [x] 1.13 `get_order_by_id` 正常返回状态与已成交量
- [x] 1.14 `get_order_by_id` 订单不存在 → 返回空而非抛出
- [x] 1.15 适配器无 `get_order_by_id` → `maker_first_hedge` 不抛未捕获异常
- [x] 1.16 降级路径 → 断言按仓位差补单，**不得**按下单量盲目吃单
- [x] 1.17 对冲入口依赖缺失且未声明放弃 → 拒绝启动
- [x] 1.18 运行定向测试，确认因功能缺失而预期失败（22 failed）

## 2. 适配器与对冲引擎

- [x] 2.1 `LighterClient.get_order_by_id` 实现
- [x] 2.2 `hedge_engine` 改为能力探测式读取订单状态
- [x] 2.3 降级路径：撤单后按实际仓位差补单
- [x] 2.4 运行定向测试并保持全绿（166 passed）

## 3. 做市引擎互锁

- [x] 3.1 心跳读取与存活判定（含四种失效情形）
- [x] 3.2 方向性门控：只拒扩大敞口，放行缩小敞口与翻单
- [x] 3.3 互锁状态写入心跳与实时状态文件
- [x] 3.4 触发/恢复告警与持续期限频
- [x] 3.5 纳入 `RISK_LAYER_REQUIREMENTS`（对冲模式下缺配置即拒绝启动）
- [x] 3.6 运行定向测试并保持全绿（166 passed）

## 4. 入口接线

- [x] 4.1 `tools/run_lighter_mm.py` 新增互锁参数（默认不启用）
- [x] 4.2 `tools/run_lighter_hedge.py` 接入风控自检与启动摘要
- [x] 4.3 对冲腿账户参数可配并在摘要中可见
- [x] 4.4 运行定向测试并保持全绿（166 passed）

## 5. 验证

- [x] 5.1 `openspec validate grid-hedge-interlock --strict`
- [x] 5.2 完整 `tests/` 无既有回归（782 passed）
- [x] 5.3 回放 8/19 场景：对冲心跳停在 00:08 → 断言做市互锁触发、不再开新仓
- [x] 5.4 未配置互锁时，Extended 与 Lighter 现行实参行为与变更前一致
- [x] 5.5 确认未修改 plist、未改动风控阈值与算法、未访问真实网络、未提交推送
