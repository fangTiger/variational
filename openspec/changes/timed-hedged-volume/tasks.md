## 0. 前置

- [x] 0.1 记录完整 `tests/` 基线通过数（809 passed）

## 1. 失败路径测试（先写，必须先失败）

- [x] 1.1 持仓未满周期 → 不平仓；已满 → 平仓
- [x] 1.2 周期内已有持仓 → 不追加开仓
- [x] 1.3 方向逐轮交替；重启后依记录延续、不重复上轮方向
- [x] 1.4 开仓完成后净敞口在容差内；平仓后两侧均为零（P0 修复后允许明确告警的不可交易残余）
- [x] 1.5 Lighter 已开仓、Extended 对冲超时未成 → **回滚 Lighter**，断言净敞口归零
- [x] 1.6 Extended 已建仓、Lighter 未成交 → **平掉 Extended**
- [x] 1.7 任一侧部分成交 → 按两侧实际持仓差补齐，**不得按委托量推断**
- [x] 1.8 重启且记录未到期 → 不重复开仓
- [x] 1.9 重启且记录已过期 → 立即平仓
- [x] 1.10 持久化状态与实际持仓不符 → 以实际为准收敛并告警
- [x] 1.11 对冲失效 → 跳过开仓；已有持仓到期 → 仍平仓
- [x] 1.12 下单走 maker 优先，超时才市价
- [x] 1.13 运行定向测试，确认因功能缺失而预期失败（13 failed）

## 2. 策略实现

- [x] 2.1 轮次调度：开仓、计时、到期平仓
- [x] 2.2 方向交替与状态持久化
- [x] 2.3 两侧同步下单，复用 `maker_first_hedge`
- [x] 2.4 单边成交收敛（回滚 / 补齐，按实际持仓差）
- [x] 2.5 对冲存活互锁接线
- [x] 2.6 运行定向测试并保持全绿

## 3. 入口

- [x] 3.1 `tools/run_timed_volume.py`：周期、名义额、初始方向、maker 超时等参数
- [x] 3.2 启动摘要输出参数与当前轮次状态
- [x] 3.3 心跳写入，含轮次、方向、到期时刻、净敞口、互锁状态
- [x] 3.4 运行定向测试并保持全绿（43 passed，含策略与入口）

## 4. 验证

- [x] 4.1 `openspec validate timed-hedged-volume --strict`
- [x] 4.2 完整 `tests/` 无既有回归（852 passed，基线 809）
- [x] 4.3 回放：2 小时周期 + $2000 + 多空交替，断言一天 12 轮、刷量 $48,000
- [x] 4.4 确认未修改 plist、未改动网格与对冲引擎既有行为、未访问真实网络、未提交推送

## 5. P0 跨所数量容差修复（2026-08-21）

- [x] 5.1 事故回放先行：Lighter `0.00129` / Extended `-0.00128` 在旧实现返回 `execution_uncertain`
- [x] 5.2 两侧差一个数量步长 → 使用适配器真实最小下单量推导容差并正常推进轮次
- [x] 5.3 净敞口达到或超出容差 → 仅按重读实仓补齐较小腿；容差内部分成交不追加目标缺口单
- [x] 5.4 补齐量低于目标侧最小下单量 → 不下单、中文告警并确定性收敛
- [x] 5.5 平仓残余低于各侧最小下单量 → 结束轮次且不重复提交；达到最小量的残余不得被配置容差掩盖
- [x] 5.6 任一侧最小下单量查询失败 → 互锁、普通轮询且不进入 `execution_uncertain`
- [x] 5.7 测试桩继续逐项核对 `LighterClient` 与 `ExtendedClient` 的真实异步方法签名
- [x] 5.8 `openspec validate timed-hedged-volume --strict`
- [x] 5.9 完整 `tests/` 无回归（862 passed，基线 852）
- [x] 5.10 确认未修改 plist、`grid_engine`、`hedge_engine` 与风控阈值，未访问真实网络、未提交推送
