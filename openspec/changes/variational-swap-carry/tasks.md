> **本任务表已按三方评审收敛结论重排（2026-09-04）。**
> 核心变化：阶段 3/4/5 冻结；先用最低成本回答「有没有硬顶」与「edge 是否真实」，
> 再决定是否投入生产工程。

## 0. 判决性取证（最高优先级，可能单方面终结提案）
- [x] 0.1 **每小时采样器**：记录 XAU `predicted_funding_rate`、XAUS `long/short_rate`、
      两腿 OI、基差（mark 与 index 各自的差）、固定名义 RFQ 点差、
      `trading_sessions` 新鲜度。**首要目的：判定 XAU 是否存在 +10.95%/年 硬顶**
      （528 条 `perpetual_future` 观测 0 条突破；RWA 类 SNDK 已印 48.79%，
      但 XAU 本身账户样本数为 0）。一周内可出结论
- [ ] 0.2 若观测到 f_perp 反复精确停在某常数 → **终止提案**并归档结论
- [ ] 0.3 观察 f_perp 是否收敛到 f_swap（5.72%）附近的地板；
      若是，则该结构按构造已死，采样再久也无用

## 1. 生产 bug（与本提案解耦，独立优先做）
- [ ] 1.1 **Extended 侧资金费单位校准**：`tracking/monitor.py:52` 的「小数/1 小时」
      假设从未校准，按新口径会翻转现有 BTC 结构的方向建议。
      调用点 `tools/hedge.py:74`、`tracking/track_equity_util.py:33`
- [ ] 1.2 仓位查询强制 `exact=True`：`get_position("XAU")` 会子串命中 `XAUS`，
      **只要碰 XAUS（哪怕只是 $5 探针仓）就会踩**；弃用 `get_liquidation_info` 自算

## 2. 最小适配层（只做取证所需，全部先写失败测试）
- [x] 2.1 失败测试：swap instrument 缺 `kind` / `funding_interval_s != 0` 必须被拒
- [x] 2.2 `_instrument` 默认值改哨兵 `None`；`_resolve_instrument_params` 支持 swap
- [x] 2.3 `/funding/swap` 封装：分别保存 `raw_rate` / `coverage_days` /
      `day_count_basis` / `normalized_annual_rate` / `observed_at`；
      **禁止用「幅度约 3 倍」启发式判定多日计提**（概念错误，见 design D2c）
- [x] 2.4 交易时段解析：**必须解析 `trading_sessions`，禁止写死 21:00Z**
      （实测下周一 09-07 为 18:30Z 收市，上线第 5 天日历已不规则）；
      元数据缺失或陈旧时判为不可交易
- [x] 2.5 收益模型纯函数：`weekly_flat` 与 `hold_through` 并列计算 R，
      两腿比例系数分别计算（永续 0.705 / swap 4/7），含 6bp 成本口径；
      基差由采样器独立记录，本阶段不引入需要历史状态的基差变化项
- [x] 2.6 假客户端默认对未配置调用**抛错**；成交/失败必须由测试显式编排

## 3. $5 探针仓（手工执行，不为它建状态机，须在放行 IP 上）
- [ ] 3.1 同一交易时段内开平，实测配对往返成本分布（取 p90 校准 6bp 口径）
- [ ] 3.2 观察一次普通日 21:05Z 结算，校验实际扣款与预测一致（闭合单位证据链缺口）
- [ ] 3.3 **跨一个周末**，实测 swap 多日计提方向（周五收 3 天 还是 周一追补）——
      这是摆动幅度最大的未知项
- [ ] 3.4 实测 XAUS 真实 IM（`min_im 0.10` 与报价 0.05 的矛盾）与真实强平价
- [ ] 3.5 顺带观测 OI 积分是否对两腿独立计数（不为它单独开仓、不因它放大规模）

## 4. 【冻结】生产化工程
> 解冻条件（须**同时**满足）：
> (i) 采样证明 f_perp 存在稳定高于 15% 的地板且无硬顶；
> (ii) 账户可投资金 ≥ $10k。
> 当前两条都不满足。
>
> 说明：D3/D4 那套状态机不是为保护收益，而是防「第一腿成交后崩溃、
> 裸 isolated 腿跨 49 小时休市」——一次事故损失约等于 1~3 年全部利润。
> **工程量下限由失败模式决定，不由 P&L 决定；所以正确答案是暂不跑这个策略，
> 而不是把工程做薄。**
- [ ] 4.1 持久化状态机 + WAL + 幂等键
- [ ] 4.2 完整故障矩阵与崩溃恢复演练
- [ ] 4.3 单写者锁
- [ ] 4.4 自动入场/退出、pre-close 冻结窗口、单腿失衡处置
- [ ] 4.5 硬名义上限 + kill switch + 告警送达验证
- [ ] 4.6 周末持仓政策（须先解决 design D2b 的三项前置条件）

## 4A. 用户明确授权的只减仓守护（不解冻自动入场）
> 2026-09-05 用户基于无远程告警与既往无声停摆事故，明确要求先部署
> fail-closed 自动降险。此范围只处理已有人工作出的仓位，不改变阶段 4 的
> 自动入场冻结状态。
- [x] 4A.1 每五分钟 `--once` 风控守护：kill switch、失衡、强平距离与长休市
- [x] 4A.2 权威强平价或时段元数据不可用时自动平仓；每日一小时休市继续持有
- [x] 4A.3 有限平仓重试、认证/地区错误本地通知、显著事故状态与连续失败计数
- [x] 4A.4 每轮原子心跳、JSONL 审计、人工 status 超过 15 分钟标红
- [x] 4A.5 离线失败路径测试与五分钟 launchd plist（仅写文件，不加载）

## 4B. 用户明确授权的守护进程自动开仓
> 2026-09-05 用户进一步授权在现有守护进程内完成周日开市后的自动重开。
> 此范围只复用已经实盘验证的 `cmd_open` 两腿执行与回滚原语，不解冻通用策略
> 状态机、自动补腿或自动翻向。
- [x] 4B.1 自动开仓置于全部平仓/风控分支之后，仅允许两腿均为空仓时进入
- [x] 4B.2 失败关闭校验交易时段、距休市 2 小时、净 carry 5%、保证金、
      kill switch、每日尝试上限与 $3,000 硬名义上限
- [x] 4B.3 默认每腿 $500，支持命令行与 `AUTO_OPEN_NOTIONAL_USD` 覆盖，
      支持 `--no-auto-open` 和 `--dry-run`
- [x] 4B.4 精确区分 message 含 `skew` 的 HTTP 422；记录但不通知，下一轮可重试，
      每日最多尝试 20 次
- [x] 4B.5 第二腿失败复用 `reduce_only` 回滚；回滚失败持久化 `INCIDENT` 并阻止后续开仓
- [x] 4B.6 心跳与审计记录开仓尝试、结论和当日计数；全部失败路径使用离线严格假客户端覆盖

## 5. 决策点
- [ ] 5.1 采样满 30 天后作 go / no-go：保守净收益是否仍 ≥ 5%/年
- [ ] 5.2 更新中文文档并归档结论（无论继续还是终止）
