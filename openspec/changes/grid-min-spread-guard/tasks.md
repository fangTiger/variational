## 1. 失败路径测试（先写，必须先失败）

- [x] 1.1 盘口读取失败/超时/返回空 → 挂单仍放行，不跳过任何格
- [x] 1.2 SELL 价 ≤ ask → 跳过、不进重试队列
- [x] 1.3 BUY 价 ≥ bid → 跳过、不进重试队列
- [x] 1.4 同格连续 CANCELLED 达阈值 → 进入退避、停止重挂
- [x] 1.5 退避期满 → 恢复挂单；期间成交 → 计数归零
- [x] 1.6 单轮多笔挂单 → 断言盘口只读取 1 次（用桩计数）
- [x] 1.7 正确一侧的挂单 → 断言逐单行为与变更前一致
- [x] 1.8 post-only 取消 → 不重挂；普通取消 → 仍重挂
- [x] 1.9 运行定向测试，确认因功能缺失而预期失败

## 2. 适配器实现

- [x] 2.1 `ExtendedClient` 复用既有真实盘口方法；异常由引擎按不可用降级
- [x] 2.2 `LighterClient` 新增同签名方法；先验证 orderbook 端点可用性
- [x] 2.3 Lighter 端点已有人工 HTTP 200 实测，本项降级分支不适用
- [x] 2.4 运行定向测试并保持全绿

## 3. 引擎实现

- [x] 3.1 `adapters/lighter_order.py` 状态映射保留取消原因，不再压平
- [x] 3.2 `run_once` 每轮取一次盘口存入本轮缓存
- [x] 3.3 `_place` 前置市价侧校验，触发时跳过且不入重试队列
- [x] 3.4 按取消原因分流：post-only 不重挂；连续计数与退避兜底，成交时归零
- [x] 3.5 中文日志：跳过原因、退避进入/退出、盘口降级告警
- [x] 3.6 运行定向测试并保持全绿

## 4. 验证

- [x] 4.1 `openspec validate grid-min-spread-guard --strict`
- [x] 4.2 运行完整 `tests/` 确认无既有回归
- [x] 4.3 回放 8/19 场景（市价 64,418 / 卖格 64,233~64,297），确认全部跳过且无死循环
- [x] 4.4 确认未修改 plist、未改动现行策略参数、未访问真实网络、未提交推送
