## 0. 前置

- [x] 0.1 记录完整 `tests/` 基线通过数
- [x] 0.2 `requirements.txt` 新增 `hyperliquid-python-sdk` 并注明 eth-utils 降级影响

## 1. 失败路径测试（先写，必须先失败）

- [x] 1.1 默认构造下调用下单/撤单 → 抛中文错误，无写请求
- [x] 1.2 声明交易但缺凭据 → 构造或连接即失败
- [x] 1.3 行情返回 bid ≥ ask 或非正数 → 视为无效并抛出
- [x] 1.4 标记价不得用盘口中值替代（断言取的是交易所标记价字段）
- [x] 1.5 查询余额 → 可经 `equity` 取到正值
- [x] 1.6 无仓时持仓为零；有仓时带正确符号
- [x] 1.7 账户级持仓/挂单不按标的过滤
- [x] 1.8 限价单只挂单语义；市价单只减仓语义
- [x] 1.9 撤销已成交/已撤订单 → 视为成功不抛异常
- [x] 1.10 `get_order_by_id` 不存在 → 返回空；底层报错 → 抛出
- [x] 1.11 最小下单量为正；价格与数量取整符合精度
- [x] 1.12 运行定向测试，确认因功能缺失而预期失败

## 2. 适配器实现

- [x] 2.1 凭据装配：支持 API 代理钱包，主钱包私钥非必需
- [x] 2.2 只读方法：持仓、全部持仓、盘口、标记价、余额、挂单、全部挂单
- [x] 2.3 订单方法：限价单、市价单、撤单、按 ID 查询、历史订单
- [x] 2.4 元数据：最小下单量、价格精度、取整
- [x] 2.5 清算信息（用于硬止损与 TPSL 约束）
- [x] 2.6 运行定向测试并保持全绿

## 3. 验证

- [x] 3.1 `openspec validate hyperliquid-adapter --strict`
- [x] 3.2 完整 `tests/` 无既有回归
- [x] 3.3 断言真实适配器实现了统一接口要求的全部方法（防桩与实现漂移）
- [x] 3.4 确认未修改 plist、未改动风控与现有适配器、未访问真实网络、未提交推送

## 4. 统一账户权益修复

- [x] 4.1 按 SDK 0.24.0 的 `spot_user_state(address)` 与
  `balances[].coin/total/hold/entryNtl` 结构补测试桩及签名断言
- [x] 4.2 先补测试并确认 RED：覆盖永续 0 + Spot USDC 457.31、两侧求和、
  双零拒绝、两种单侧失败、双侧失败、排除 HYPE/MAX
- [x] 4.3 `get_balance()` 合计永续账户权益与 Spot USDC，并返回两部分诊断字段
- [x] 4.4 单侧失败按零降级；双侧均失败或权益合计非正时抛出
- [x] 4.5 定向测试、离线实盘场景回放与完整 `tests/` 全绿
- [x] 4.6 `openspec validate hyperliquid-adapter --strict` 通过，并核对未越界修改
