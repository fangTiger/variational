## 1. 失败路径测试（先写，必须确认 RED）

- [x] 1.1 测试桩模拟 Variational reduce-only 平仓抛受限地区异常、Hyperliquid 正常成交
- [x] 1.2 连续失败达到三次后进入 `close_halted` 并持久化计数、起点
- [x] 1.3 退避后续节拍断言 `_flatten_all` 调用计数不再增加
- [x] 1.4 退避期间断言不开新轮次
- [x] 1.5 退避期满重试成功后断言失败计数归零、退避起点清空
- [x] 1.6 退避期间出现净敞口时，每个退避周期最多再对冲一次，且重启不恢复额度
- [x] 1.7 进入退避通过既有告警通道只发一次中文告警
- [x] 1.8 代理警告与无代理双实例脚本行为测试确认 RED

## 2. 最小实现

- [x] 2.1 增加连续失败阈值、退避时长常量、两个指定持久化字段和跨重启再对冲限次标记
- [x] 2.2 平仓成功清状态，失败累计并持久化，达到阈值时进入退避
- [x] 2.3 退避未满时绕过 `_flatten_all`、普通实仓收敛和开仓路径
- [x] 2.4 退避期满放行一次平仓；失败重置计时，成功恢复正常
- [x] 2.5 复用既有 `_warn` 告警通道，并限制为每个退避周期一次
- [x] 2.6 增加退避周期内最多一次的净敞口再对冲
- [x] 2.7 增加 CLI 代理环境醒目中文警告
- [x] 2.8 增加 SNDK/BTC 共用的 `scripts/run_timed_volume.sh`

## 3. 验证

- [x] 3.1 `openspec validate fix-timed-volume-close-backoff --strict`
- [x] 3.2 定向策略与 CLI/脚本测试全部通过
- [x] 3.3 `bash -n scripts/run_timed_volume.sh` 通过
- [x] 3.4 `PYTHONPATH=. .venv/bin/python -m pytest tests/ -q` 全绿且无既有回归（1103 passed）
- [x] 3.5 tasks 状态、规格场景与实际实现逐项一致
- [x] 3.6 确认未触碰 `data/`、未重启或终止任何进程、未修改任务外文件
