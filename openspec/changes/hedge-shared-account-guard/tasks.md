## 1. 失败路径测试（先写，必须先失败）

- [x] 1.1 未声明 + 账户前缀为 X10_GRID → 拒绝（与变更前一致）
- [x] 1.2 未声明 + vault 与网格相同 → 拒绝
- [x] 1.3 已声明 + Extended 可交易仓位无法由 Lighter primary 解释 → 拒绝，错误包含两侧数量与净敞口
- [x] 1.4 已声明 + 有普通网格挂单 → 无论持仓是否对冲均拒绝，错误说明仍有网格活动
- [x] 1.5 已声明 + 0.00003 微仓低于市场最小下单量且无网格挂单 → 放行
- [x] 1.6 启动摘要标注共用模式
- [x] 1.7 运行定向测试，确认因功能缺失而预期失败
- [x] 1.8 最小下单量查询失败 → 使用保守极小阈值并记录中文日志
- [x] 1.9 真实 Extended 适配器公开最小下单量查询方法，测试不只依赖桩

## 2. 实现

- [x] 2.1 新增显式共用声明参数，默认关闭
- [x] 2.2 声明后跳过前缀/vault 硬拒绝，转入运行时校验
- [x] 2.3 运行时校验：按既有再平衡比例核对两腿净敞口，忽略不可交易微仓，保留普通网格挂单拒绝
- [x] 2.4 启动摘要标注共用模式
- [x] 2.5 运行定向测试并保持全绿
- [x] 2.6 最小下单量由适配器市场元数据提供，查询失败时记录日志并保守回退

## 3. 验证

- [x] 3.1 `openspec validate hedge-shared-account-guard --strict`
- [x] 3.2 完整 `tests/` 无既有回归（基线 791，完成后 794 passed）
- [x] 3.3 未声明时三层静态拒绝行为与变更前逐项一致
- [x] 3.4 确认本次未修改 plist、未改动风控阈值与对冲算法、未访问真实网络、未提交推送

## 4. P0 纠偏：合法对冲仓位不得阻塞重启

- [x] 4.1 更新 proposal/spec，撤销已被实盘证伪的「正常规模仓位一律拒绝」表述，记录事故教训
- [x] 4.2 先写事故回放：Extended `+0.01972` / Lighter `-0.01972` 在 `0.02` 容差内放行，并确认 RED
- [x] 4.3 覆盖 Extended 有仓而 Lighter 为零、两腿同向、普通网格挂单、微仓、未声明静态拒绝及任一侧读取失败
- [x] 4.4 核对真实 Lighter/Extended 适配器均公开校验所调用的只读持仓方法，测试不得只依赖桩
- [x] 4.5 最小实现两腿净敞口校验，直接复用 `--rebalance-threshold`，不改风控阈值、对冲算法或网格策略
- [x] 4.6 `openspec validate hedge-shared-account-guard --strict` 通过
- [x] 4.7 定向测试与完整 `tests/` 全绿，完成后核对 tasks 与实现一致
- [x] 4.8 确认未触碰已有 plist 改动、未执行 launchctl、未访问真实网络、未提交推送
