## 0. 前置

- [x] 0.1 记录完整 `tests/` 基线通过数（733 passed）

## 1. 失败路径测试（先写，必须先失败）

- [x] 1.1 本机时区设为非 UTC+8 → 判定仍以 UTC+8 为准
- [x] 1.2 窗口起点/终点整分的归属明确且可测
- [x] 1.3 跨日窗口与跨零点的判定正确
- [x] 1.4 周六、周日与工作日判定完全一致
- [x] 1.5 进入停机 → 断言「先撤单、后平仓」的顺序
- [x] 1.6 停机期间 → 断言不产生任何扩大敞口的挂单
- [x] 1.7 进入停机且库存为零 → 断言不产生任何平仓委托
- [x] 1.8 maker 平仓超时未成交 → 断言撤单并以市价补齐未成交部分
- [x] 1.9 maker 完全未成交 → 断言**绝不**保留库存进入停机
- [x] 1.10 平仓抛异常 → 告警且下轮继续重试，**不得**静默进入停机
- [x] 1.11 库存未归零 → 每轮持续尝试直到为零
- [x] 1.12 停机期间心跳继续输出且可识别为计划内停机
- [x] 1.13 时间进入窗口 → 自动恢复铺网格，无需重启
- [x] 1.14 停机时段启动 → 不铺网格，且接管的非零库存被平掉
- [x] 1.15 离场平仓 → 断言**不写入** `halted`，次日可自动恢复
- [x] 1.16 未配置窗口 → 逐单行为与变更前完全一致
- [x] 1.17 运行定向测试，确认因功能缺失而预期失败（18 failed）

## 2. 引擎实现

- [x] 2.1 UTC+8 固定偏移的时段判定（不读本机时区）
- [x] 2.2 离场序列：撤单 → 平仓至零 → 停止开仓
- [x] 2.3 maker 优先平仓 + 超时转市价，超时可配
- [x] 2.4 平仓失败持续重试 + 限频告警
- [x] 2.5 窗口外保持存活与心跳，窗口开始自动恢复
- [x] 2.6 停机时段启动：不铺网格并平掉接管库存
- [x] 2.7 计划内平仓与 `halted` 路径分离
- [x] 2.8 运行定向测试并保持全绿（窗口/钱包/CLI/自检 83 passed）

## 3. 入口接线

- [x] 3.1 `tools/run_grid.py` 新增窗口参数（默认不启用）
- [x] 3.2 `tools/run_lighter_mm.py` 新增窗口参数（默认不启用）
- [x] 3.3 启动摘要体现窗口配置与当前时段状态
- [x] 3.4 运行定向测试并保持全绿（83 passed）

## 4. 验证

- [x] 4.1 `openspec validate grid-trading-window --strict`
- [x] 4.2 完整 `tests/` 无既有回归（758 passed；基线 733 + 新增 25）
- [x] 4.3 回放 8/19：20:00 离场 → 断言避开 20–24 点的 +3,666 涨幅段
- [x] 4.4 不传窗口参数时，Extended 与 Lighter 现行实参行为与变更前一致
- [x] 4.5 确认未修改 plist、未改动其他风控阈值与算法、未访问真实网络、未提交推送
