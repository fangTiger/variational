## 1. maker 优先下单函数

- [x] 1.1 先写方向、全成、超时、部分成交、撤单竞态、零变化量和真实接口契约测试并确认失败
- [x] 1.2 实现按订单状态轮询、撤单后重读和按剩余量吃单
- [x] 1.3 运行 maker 优先定向测试并保持全绿

## 2. 对冲引擎与 CLI 接入

- [x] 2.1 先写默认关闭、显式启用、reduce-only、CLI 参数/装配/摘要测试并确认失败
- [x] 2.2 仅在 `HedgeConfig` 增加配置，并仅修改 `_rebalance` 的下单分流
- [x] 2.3 增加 CLI 参数、配置装配和启动摘要展示
- [x] 2.4 运行既有对冲名义上限、CLI 与 maker 优先测试并保持全绿

## 3. 最终验证

- [x] 3.1 对照 spec、实施计划和禁止范围复核差异
- [x] 3.2 运行 `.venv/bin/python -m pytest tests/ -q`，通过数不得低于 562 加新增用例
- [x] 3.3 确认未发送真实网络请求、未重启或终止实盘进程、未执行 Task 3
