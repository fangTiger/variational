## 0. 前置

- [x] 0.1 确认 `grid-risk-selfcheck` 已落地且 `get_balance()` 可用
- [x] 0.2 记录完整 `tests/` 基线通过数（变更前：`712 passed in 3.50s`）

## 1. 失败路径测试（先写，必须先失败）

- [x] 1.1 权益查询失败 → 退回绝对硬顶，**不得**放行无上限挂单
- [x] 1.2 权益返回 0 / 负数 / None → 同样退回硬顶并告警
- [x] 1.3 权益不可用告警限频（断言重复发生时日志条数不线性增长）
- [x] 1.4 权益 × 比例 > 硬顶 → 上限为硬顶（不得突破）
- [x] 1.5 权益 × 比例 < 硬顶 → 上限为比例值
- [x] 1.6 权益下跌 → 上限相应收紧
- [x] 1.7 已有库存超上限 → 拒绝同向新增
- [x] 1.8 已有库存超上限 → **断言无任何平仓/减仓调用**
- [x] 1.9 库存超上限但挂单缩小敞口 → 照常放行
- [x] 1.10 未配置比例 → 逐单行为与变更前完全一致
- [x] 1.11 同一限频周期多次计算上限 → 断言权益查询次数不增加
- [x] 1.12 运行定向测试，确认因功能缺失而预期失败（RED：`12 failed in 0.07s`）

## 2. 引擎实现

- [x] 2.1 `GridConfig` 新增比例参数，默认不启用
- [x] 2.2 上限计算：比例值与硬顶取更紧者，复用权益限频缓存
- [x] 2.3 权益不可用时退回硬顶 + 限频告警
- [x] 2.4 `_within_cap` 改用计算后的上限，**判定语义保持不变**
- [x] 2.5 `_log_cap_rejection` 日志体现上限来源（比例值 or 硬顶）
- [x] 2.6 把 `get_balance` 纳入库存上限层的风控自检依赖映射
- [x] 2.7 运行定向测试并保持全绿

## 3. 入口接线

- [x] 3.1 `tools/run_grid.py` 新增比例参数
- [x] 3.2 `tools/run_lighter_mm.py` 新增比例参数
- [x] 3.3 风控启动摘要中体现库存上限的来源与当前生效值
- [x] 3.4 运行定向测试并保持全绿

## 4. 验证

- [x] 4.1 `openspec validate grid-wallet-exposure-cap --strict`
- [x] 4.2 完整 `tests/` 无既有回归（变更后：`733 passed in 3.36s`）
- [x] 4.3 回放 8/19 数值：权益 $730 + 比例 → 断言上限显著低于 $3,750（$729.76 × 3 = $2,189.28）
- [x] 4.4 不传比例参数时，Extended 与 Lighter 现行实参行为与变更前一致
- [x] 4.5 确认未修改 plist、未改动其他风控阈值与算法、未访问真实网络、未提交推送
