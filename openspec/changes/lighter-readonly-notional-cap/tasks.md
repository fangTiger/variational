## 1. Lighter 只读适配器

- [x] 1.1 先写 `sign=-1` 的失败测试并确认按预期失败
- [x] 1.2 补齐其余持仓、失败、未开户和只读交易约束测试并确认失败
- [x] 1.3 实现账户连接、严格持仓解析、行情和清算信息读取及资源释放
- [x] 1.4 实现明确拒绝下单与平仓的只读约束
- [x] 1.5 运行 `tests/test_lighter_client.py` 并保持全绿

## 2. 引擎泛化与名义上限

- [x] 2.1 先写超限不下单和保留已有对冲仓的失败测试并确认失败
- [x] 2.2 补齐等于上限和禁用上限的向后兼容测试并确认失败
- [x] 2.3 实现 `max_primary_notional` 再平衡门禁，超限时拒绝跟单并保留更早执行的降险保护
- [x] 2.4 仅用显式配置的异常类型识别自愈异常，并保留 Variational 入口的既有自愈逻辑
- [x] 2.5 为人工只读 primary 禁止非原子双平/双减仓并返回人工告警
- [x] 2.6 运行新增测试与既有对冲引擎测试并保持全绿

## 3. 最终验证

- [x] 3.1 对照 spec 与实施计划核对实现和任务状态
- [x] 3.2 运行 `.venv/bin/python -m pytest tests/ -q`，通过数不得低于 288 加新增用例
- [x] 3.3 检查工作区差异，确认未修改禁止范围且未发送真实网络请求

## 4. 代码审查修复

- [x] 4.1 用真实业务成功码 `200` 更新 Lighter 成功响应桩，并兼容 `0`、缺失字段和 `None`
- [x] 4.2 让清算保护和保证金降险先于名义上限门禁执行，门禁仅阻止再平衡
- [x] 4.3 删除认证异常类名后缀兜底，要求调用方显式配置自愈异常类型
- [x] 4.4 重新运行定向测试和完整 `tests/`，并复核受保护路径
