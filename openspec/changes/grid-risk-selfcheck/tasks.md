## 1. 失败路径测试（先写，必须先失败）

- [x] 1.1 风控层启用但依赖能力缺失 → 拒绝启动，错误含缺失能力与受影响层
- [x] 1.2 未传任何放弃声明 → 默认不放弃，缺失即拒绝
- [x] 1.3 已声明放弃 → WARNING 放行且继续启动
- [x] 1.4 dry-run 下同样拒绝，不得形成「能跑」错觉
- [x] 1.5 依赖齐备 → 正常启动，逐项行为与变更前一致
- [x] 1.6 摘要必须列出「关闭」的层，且关闭与不可用标记可区分
- [x] 1.7 运行期缺失：首次 WARNING、连续重复限频（断言日志条数）
- [x] 1.8 `get_balance` 查询失败 → 抛出异常，**不得**返回 0 权益
- [x] 1.9 `get_balance` 正常 → `equity` 可经键或属性取到正数
- [x] 1.10 回放事故配置（Lighter 三 flag 全无 + 无 `get_balance`）→ 断言拒绝启动
- [x] 1.11 运行定向测试，确认因功能缺失而预期失败

## 2. 适配器实现

- [x] 2.1 核对 Lighter 账户接口的权益口径（抵押品是否含未实现盈亏）并记录结论
- [x] 2.2 `LighterClient.get_balance()` 实现，文档字符串写明口径
- [x] 2.3 运行定向测试并保持全绿

## 3. 引擎实现

- [x] 3.1 建立风控层 → 依赖能力的显式映射表
- [x] 3.2 启动自检：缺失且未声明放弃则拒绝启动，中文错误
- [x] 3.3 知情放弃开关（默认全不放弃）
- [x] 3.4 启动风控状态摘要，逐层输出、区分关闭与不可用
- [x] 3.5 运行期缺失改为限频告警，复用既有聚合方式
- [x] 3.6 运行定向测试并保持全绿

## 4. 入口接线

- [x] 4.1 `tools/run_lighter_mm.py` 接入自检与摘要
- [x] 4.2 `tools/run_grid.py` 接入自检与摘要，确认 Extended 现行参数可正常启动
- [x] 4.3 运行定向测试并保持全绿

## 5. 验证

- [x] 5.1 `openspec validate grid-risk-selfcheck --strict`
- [x] 5.2 完整 `tests/` 无既有回归（先记录基线）
- [x] 5.3 用 Extended 现行 plist 实参跑自检 → 必须通过（不得误伤既有配置）
- [x] 5.4 用 Lighter 现行 plist 实参跑自检 → 必须拒绝并指出三层缺失
- [x] 5.5 确认未修改任何 plist、未改动风控阈值与算法、未访问真实网络、未提交推送
