## 1. 适配器契约统一

- [x] 1.1 创建契约测试并确认旧签名按预期失败
- [x] 1.2 在基类声明 `cancel_order(market, order_id)`
- [x] 1.3 调整 Extended 与 Variational 的公开参数并同步内部调用，保持底层撤单行为不变
- [x] 1.4 只修改网格引擎唯一撤单调用行，并同步旧测试假对象
- [x] 1.5 运行契约测试与完整测试集

## 2. 精度换算

- [x] 2.1 创建精度换算测试并确认模块缺失导致预期失败
- [x] 2.2 实现 Decimal 整数换算、向下截断和最小量校验
- [x] 2.3 运行精度换算测试

## 3. 订单号映射层

- [x] 3.1 创建订单引用、持久化分配器和解析测试并确认预期失败
- [x] 3.2 实现 `OrderRef`、跨重启分配器和活动订单解析
- [x] 3.3 运行订单号映射测试

## 4. 最终验证

- [x] 4.1 对照 spec、实施计划和实际差异复核 Task 1–3 范围
- [x] 4.2 运行 `.venv/bin/python -m pytest tests/ -q`，通过数不得低于 505 加新增用例
- [x] 4.3 重跑 `cancel_order` 全局检索，逐项确认签名与调用处理
- [x] 4.4 确认 `grid/grid_engine.py` 严格只有唯一撤单调用行发生变化
- [x] 4.5 确认未触碰禁区、未访问真实网络、未提交或推送
