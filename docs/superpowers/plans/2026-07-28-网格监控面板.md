# 网格监控面板 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.
>
> **执行分工**：实现代码由 **Codex（gpt-5.5，reasoning=xhigh，`workspace-write`）** 编写，Claude 全程盯审——逐任务交 Codex，拿回产出后审查 + 跑测试验证。见记忆 `codex-implements-claude-reviews`。

**Goal:** 一个本地 Web 监控面板，浏览器打开 `localhost:8787` 一眼看到当前网格的市况/趋势(ADX+斜率+ATR%)、band 区间、盈亏、风险；趋势判断与解读全用指标纯函数，**零 LLM**。

**Architecture:** 引擎 `run_once_trend_aware` 末尾把当前状态原子写 `data/grid_live.json`；`tools/grid_panel.py`（Python stdlib `http.server`，无第三方依赖）读该文件 + `grid_monitor.jsonl` 最新条，渲染深色 HTML，页面 `<meta refresh>` 每 10 秒自刷新。面板不连交易所。

**Tech Stack:** Python 3.11、stdlib `http.server`、pytest（`PYTHONPATH=. .venv/bin/python -m pytest`）。指标复用 `grid/regime.py` 的 `ema/_wilder_atr`。

**设计依据:** `docs/superpowers/specs/2026-07-28-网格监控面板-design.md`。

---

## 文件结构

**新建：**
- `tools/grid_panel.py` — stdlib HTTP 服务 + `render_html(live, equity)` 纯渲染函数。单一职责：面板服务与渲染。
- `tests/test_panel.py` — `close_slope`/`describe_regime`/`render_html`/live-dump 的测试。

**修改：**
- `grid/regime.py` — 新增纯函数 `close_slope(closes, period)`、`describe_regime(mode, adx, slope_short, slope_long, atr_pct)`。
- `grid/grid_engine.py` — `run_once_trend_aware` 末尾新增 `_dump_live(...)`，原子写 `data/grid_live.json`（`trend_aware=False` 不写）。

---

## Task 1: 近端归一化斜率 `close_slope`

**Files:**
- Modify: `grid/regime.py`（末尾追加）
- Test: `tests/test_panel.py`

- [ ] **Step 1: 写失败测试**

```python
# tests/test_panel.py
"""监控面板纯函数与渲染测试。"""
from __future__ import annotations

from grid.regime import close_slope


def test_close_slope_flat_is_zero() -> None:
    assert abs(close_slope([100.0] * 30, 20)) < 1e-9


def test_close_slope_up_is_positive() -> None:
    closes = [100.0 + i for i in range(30)]  # 每根 +1
    s = close_slope(closes, 20)
    assert s > 0
    # 归一化：每根涨幅 ≈ 1/当前价
    assert abs(s - (1.0 / closes[-1])) < 1e-6


def test_close_slope_down_is_negative() -> None:
    closes = [200.0 - i for i in range(30)]
    assert close_slope(closes, 20) < 0


def test_close_slope_short_series_returns_zero() -> None:
    assert close_slope([100.0, 101.0], 20) == 0.0
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd /Users/captain/python/variational && PYTHONPATH=. .venv/bin/python -m pytest tests/test_panel.py -v`
Expected: FAIL（`ImportError: cannot import name 'close_slope'`）

- [ ] **Step 3: 实现（追加到 `grid/regime.py` 末尾）**

```python
def close_slope(closes: list[float], period: int) -> float:
    """近端收盘斜率，归一化为"每根变化占当前价比例"（%/根）。

    数据不足或当前价非正返回 0.0。正=上行，负=下行，≈0=走平。
    """
    if len(closes) <= period or closes[-1] <= 0:
        return 0.0
    return (closes[-1] - closes[-1 - period]) / period / closes[-1]
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cd /Users/captain/python/variational && PYTHONPATH=. .venv/bin/python -m pytest tests/test_panel.py -v`
Expected: PASS（4 passed）

- [ ] **Step 5: 提交**

```bash
git add grid/regime.py tests/test_panel.py
git commit -m "feat(panel): 新增近端归一化斜率 close_slope 纯函数

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 2: 规则市况解读 `describe_regime`

**Files:**
- Modify: `grid/regime.py`（末尾追加）
- Test: `tests/test_panel.py`（追加）

**契约:** 纯规则拼句，无 LLM。输入 mode(GridMode 或其 value 字符串)+ 三指标，输出一句中文解读。

- [ ] **Step 1: 追加失败测试**

```python
from grid.regime import GridMode, describe_regime


def test_describe_neutral_calm() -> None:
    s = describe_regime(GridMode.NEUTRAL, adx=22.0, slope_short=0.0, slope_long=0.0, atr_pct=0.004)
    assert "震荡" in s and "适合网格" in s


def test_describe_trend_off() -> None:
    s = describe_regime(GridMode.OFF, adx=35.0, slope_short=0.01, slope_long=0.008, atr_pct=0.02)
    assert "趋势" in s and "停铺" in s


def test_describe_mentions_slope_direction() -> None:
    up = describe_regime(GridMode.NEUTRAL, adx=20.0, slope_short=0.02, slope_long=0.02, atr_pct=0.004)
    assert "上行" in up
    down = describe_regime(GridMode.NEUTRAL, adx=20.0, slope_short=-0.02, slope_long=-0.02, atr_pct=0.004)
    assert "下行" in down
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd /Users/captain/python/variational && PYTHONPATH=. .venv/bin/python -m pytest tests/test_panel.py -v`
Expected: FAIL（`ImportError: cannot import name 'describe_regime'`）

- [ ] **Step 3: 实现（追加到 `grid/regime.py` 末尾）**

```python
def describe_regime(mode, adx, slope_short, slope_long, atr_pct) -> str:
    """规则拼句的中文市况解读（无 LLM）。mode 接受 GridMode 或其 value 字符串。"""
    m = mode.value if isinstance(mode, GridMode) else str(mode)
    # 斜率方向（两周期同向才明确表态，用较小量级判走平）
    thr = 0.001  # 每根 0.1% 以内算走平
    def _dir(s: float) -> int:
        return 1 if s > thr else (-1 if s < -thr else 0)
    ds, dl = _dir(slope_short), _dir(slope_long)
    if ds == dl == 1:
        slope_txt = "斜率上行"
    elif ds == dl == -1:
        slope_txt = "斜率下行"
    else:
        slope_txt = "斜率接近零"
    # 波动
    if atr_pct < 0.003:
        atr_txt = "波动小"
    elif atr_pct > 0.01:
        atr_txt = "波动大"
    else:
        atr_txt = "ATR 适中"
    head = "震荡 · 适合网格" if m == "neutral" else "趋势 · 已停铺"
    return f"{head}｜ADX {adx:.1f} · {slope_txt} · {atr_txt}"
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cd /Users/captain/python/variational && PYTHONPATH=. .venv/bin/python -m pytest tests/test_panel.py -v`
Expected: PASS（7 passed）

- [ ] **Step 5: 提交**

```bash
git add grid/regime.py tests/test_panel.py
git commit -m "feat(panel): 新增规则市况解读 describe_regime 纯函数（零LLM）

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 3: 引擎写实时状态 `data/grid_live.json`

**Files:**
- Modify: `grid/grid_engine.py`（`_run_once_trend_aware` 末尾 + 新增 `_dump_live`）
- Test: `tests/test_panel.py`（追加）

**契约（设计"数据契约"节）:**
- 新增 `_dump_live(self, mark, mode, inv, closes)`：收集当前快照，用 `grid.grid_state` 的原子写方式（临时文件 + `os.replace`）写 `self.config.state_path` 同目录下的 `grid_live.json`（即 `data/grid_live.json`）。
- 字段：`ts / mark / mode(str) / adx / slope_short / slope_long / atr_pct / inv_btc / inv_usd / band_low / band_high / frozen / blocked_side / halted / dist_to_liq_pct / cfg`。
  - `adx`：`adx(highs,lows,closes)[-1]`（在 `_run_once_trend_aware` 里已有 K 线，取最后值）。
  - `slope_short = close_slope(closes, 6)`，`slope_long = close_slope(closes, 24)`。
  - `atr_pct = self._latest_atr / mark`（`_latest_atr` 已有）。
  - `band_low/high/frozen/blocked_side/halted`：来自 `self._state`（无则 None/False）。
  - `dist_to_liq_pct`：`_check_hard_stop` 有仓时算出的距强平比例，存 `self._last_dist_to_liq`（空仓 None）；`_dump_live` 读它。
  - `cfg`：`{unit, levels, max_inv, spacing, hard_stop_dist, adx_off}` 取自 config。
- 在 `_run_once_trend_aware` **末尾（return 之前的各分支都要覆盖）** 调 `_dump_live`。dry_run 也写（本地文件，方便观察）。`trend_aware=False` 路径不写。
- `_check_hard_stop` 里：有仓且拿到 (mark, liq) 时，`self._last_dist_to_liq = dist_to_liq_pct(mark, liq, float(signed_size))`；空仓设 None。

- [ ] **Step 1: 追加失败测试**

```python
import asyncio
import json
from grid.grid_engine import GridConfig, GridEngine


def test_dump_live_writes_expected_fields(tmp_path) -> None:
    live_path = tmp_path / "grid_live.json"
    cfg = GridConfig(dry_run=True, trend_aware=True, unit_usd=40, levels_per_side=4,
                     max_inventory_usd=160, spacing_pct=0.005, hard_stop_dist=0.12,
                     adx_off=30, state_path=str(tmp_path / "grid_state.json"))
    eng = GridEngine(object(), cfg)
    eng._latest_atr = 250.0
    eng._last_dist_to_liq = None
    closes = [60000.0 + i for i in range(60)]
    from grid.regime import GridMode
    asyncio.run(eng._dump_live(mark=63000.0, mode=GridMode.NEUTRAL, inv=0.0, closes=closes))
    d = json.loads((tmp_path / "grid_live.json").read_text())
    assert d["mark"] == 63000.0 and d["mode"] == "neutral"
    assert d["cfg"]["unit"] == 40 and d["cfg"]["max_inv"] == 160
    assert "adx" in d and "slope_short" in d and "atr_pct" in d
```

- [ ] **Step 2: 跑测试确认失败**

Run: `PYTHONPATH=. .venv/bin/python -m pytest tests/test_panel.py -k dump_live -v`
Expected: FAIL（`_dump_live` 未定义）

- [ ] **Step 3: 实现** `_dump_live`（按契约收集字段 + 原子写 `grid_live.json`，路径 = `Path(config.state_path).parent / "grid_live.json"`），并在 `_run_once_trend_aware` 各返回分支前调用、在 `_check_hard_stop` 里维护 `self._last_dist_to_liq`（`__init__` 初始化为 None）。

- [ ] **Step 4: 跑测试确认通过**

Run: `cd /Users/captain/python/variational && PYTHONPATH=. .venv/bin/python -m pytest tests/test_panel.py -k dump_live -v`
Expected: PASS

- [ ] **Step 5: 跑全量确认无回归（尤其 trend-aware 引擎测试）**

Run: `cd /Users/captain/python/variational && PYTHONPATH=. .venv/bin/python -m pytest tests/ -q`
Expected: 全绿

- [ ] **Step 6: 提交**

```bash
git add grid/grid_engine.py tests/test_panel.py
git commit -m "feat(panel): 引擎每轮写 data/grid_live.json 实时状态（原子写）

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 4: 面板渲染 `render_html`

**Files:**
- Create: `tools/grid_panel.py`
- Test: `tests/test_panel.py`（追加）

**契约:** `render_html(live: dict, equity: dict) -> str` 纯函数，返回完整深色 HTML。`live` 是 grid_live.json 内容，`equity` 是 `{"equity":..., "pnl_since_start":...}`（可为空 dict）。字段缺失用 `—` 占位不崩。含 `<meta http-equiv="refresh" content="10">`。价格位置条：按 `mark` 在 `[band_low, band_high]` 的比例定位；越界（frozen 或 mark 出界）时整卡加高亮 class。

- [ ] **Step 1: 追加失败测试**

```python
from tools.grid_panel import render_html


def _live(**kw):
    base = dict(ts=0, mark=63000.0, mode="neutral", adx=22.0, slope_short=0.0,
               slope_long=0.0, atr_pct=0.004, inv_btc=0.0, inv_usd=0.0,
               band_low=60000.0, band_high=66000.0, frozen=False,
               blocked_side=None, halted=False, dist_to_liq_pct=None,
               cfg=dict(unit=40, levels=4, max_inv=160, spacing=0.005,
                        hard_stop_dist=0.12, adx_off=30))
    base.update(kw); return base


def test_render_contains_key_fields() -> None:
    html = render_html(_live(), {"equity": 286.0, "pnl_since_start": 1.2})
    assert "63000" in html and "震荡" in html and "60000" in html and "66000" in html
    assert 'refresh' in html and "286" in html


def test_render_frozen_highlights() -> None:
    html = render_html(_live(frozen=True, blocked_side="BUY", mode="off"), {})
    assert "趋势" in html or "停铺" in html
    assert "frozen" in html.lower() or "越界" in html or "冻结" in html


def test_render_missing_equity_no_crash() -> None:
    html = render_html(_live(), {})
    assert "—" in html  # 权益/盈亏缺失用占位


def test_render_no_live_data() -> None:
    html = render_html({}, {})
    assert "未运行" in html or "无数据" in html
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd /Users/captain/python/variational && PYTHONPATH=. .venv/bin/python -m pytest tests/test_panel.py -k render -v`
Expected: FAIL（`ModuleNotFoundError: tools.grid_panel`）

- [ ] **Step 3: 实现** `tools/grid_panel.py` 的 `render_html`（深色内联 CSS，用 `grid.regime.describe_regime` 生成解读句；空 live 返回"引擎未运行/无数据"页；字段缺失 `—`；价格位置条用百分比 inline style；frozen/halted 加高亮）。先只写 `render_html`（服务在 Task 5）。

- [ ] **Step 4: 跑测试确认通过**

Run: `cd /Users/captain/python/variational && PYTHONPATH=. .venv/bin/python -m pytest tests/test_panel.py -k render -v`
Expected: PASS（4 passed）

- [ ] **Step 5: 提交**

```bash
git add tools/grid_panel.py tests/test_panel.py
git commit -m "feat(panel): render_html 深色面板渲染纯函数（含价格位置条/降级）

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 5: 面板 HTTP 服务 `grid_panel.py`

**Files:**
- Modify: `tools/grid_panel.py`（在 render_html 基础上加服务 + CLI）
- Test: `tests/test_panel.py`（追加 `_load_latest_snapshot` 纯函数）

**契约:**
- 纯函数 `_read_live(path) -> dict`：读 `data/grid_live.json`，缺失/损坏返回 `{}`。
- 纯函数 `_latest_equity(jsonl_path) -> dict`：读 `grid_monitor.jsonl` 最后一行，取 `{"equity","pnl_since_start"}`；缺失返回 `{}`。
- `http.server.BaseHTTPRequestHandler` 子类：`GET /` → `render_html(_read_live(...), _latest_equity(...))`，`Content-Type: text/html; charset=utf-8`；其他路径 404。
- `main()`：`--port`(默认 8787)、`--live-path`(默认 `data/grid_live.json`)、`--monitor-path`(默认 `data/grid_monitor.jsonl`)。打印 `面板已启动：http://localhost:8787`。

- [ ] **Step 1: 追加失败测试**

```python
from tools.grid_panel import _read_live, _latest_equity


def test_read_live_missing_returns_empty(tmp_path) -> None:
    assert _read_live(tmp_path / "nope.json") == {}


def test_latest_equity_reads_last_line(tmp_path) -> None:
    p = tmp_path / "m.jsonl"
    p.write_text('{"equity":100,"pnl_since_start":1}\n{"equity":286,"pnl_since_start":2}\n',
                 encoding="utf-8")
    got = _latest_equity(p)
    assert got["equity"] == 286 and got["pnl_since_start"] == 2


def test_latest_equity_missing_returns_empty(tmp_path) -> None:
    assert _latest_equity(tmp_path / "none.jsonl") == {}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `PYTHONPATH=. .venv/bin/python -m pytest tests/test_panel.py -k "read_live or latest_equity" -v`
Expected: FAIL（函数未定义）

- [ ] **Step 3: 实现** `_read_live` / `_latest_equity` / Handler / `main()`（stdlib `http.server` + `argparse`）。

- [ ] **Step 4: 跑测试确认通过**

Run: `PYTHONPATH=. .venv/bin/python -m pytest tests/test_panel.py -v`
Expected: PASS（全部）

- [ ] **Step 5: 冒烟——起服务 curl 首页**

```bash
cd /Users/captain/python/variational
PYTHONPATH=. .venv/bin/python -m tools.grid_panel --port 8799 &
SVPID=$!; sleep 2
curl -s http://localhost:8799/ | head -c 400
kill $SVPID
```
Expected: 输出 HTML（含 `<html`、深色样式、若无 live 文件则"未运行/无数据"）

- [ ] **Step 6: 提交**

```bash
git add tools/grid_panel.py tests/test_panel.py
git commit -m "feat(panel): grid_panel HTTP 服务（stdlib，localhost:8787，10s自刷新）

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 6: 端到端验证 + 全量回归

**Files:**
- Test: 手动端到端 + `tests/` 全量

- [ ] **Step 1: 全量测试**

Run: `cd /Users/captain/python/variational && PYTHONPATH=. .venv/bin/python -m pytest tests/ -q`
Expected: 全绿（新增 test_panel + 原有不回归）

- [ ] **Step 2: 端到端冒烟（真实 grid_live.json → 面板显示）**

实盘引擎(trend-aware)已在跑，会写 `data/grid_live.json`。起面板 curl 确认真实数据渲染：
```bash
cd /Users/captain/python/variational
cat data/grid_live.json 2>/dev/null | head -c 300 || echo "(live 文件未生成，检查引擎是否 trend-aware 在跑)"
PYTHONPATH=. .venv/bin/python -m tools.grid_panel --port 8799 &
SVPID=$!; sleep 2
curl -s http://localhost:8799/ | grep -oE "震荡|趋势|ADX|band|[0-9]{5}" | head
kill $SVPID
```
Expected: 面板渲染出当前真实 mode/ADX/band/价格

- [ ] **Step 3: 提交（若端到端有微调）**

```bash
git add -A && git commit -m "test(panel): 端到端验证面板渲染真实 grid_live 数据

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>" || echo "无改动"
```

- [ ] **Step 4: 使用说明（交付给用户）**

看面板：`PYTHONPATH=. .venv/bin/python -m tools.grid_panel` → 浏览器打开 `http://localhost:8787`，页面每 10 秒自刷新，看完 Ctrl+C 停服务。

---

## Self-Review

- **Spec 覆盖**：数据契约 grid_live.json→T3；顶部/趋势卡/风险卡/参数渲染→T4；三指标(ADX/斜率/ATR%)→T1(斜率)+T3(采集)+T4(展示)；规则解读→T2；价格位置条→T4；HTTP服务10s刷新→T5；错误降级→T4/T5；端到端→T6。全覆盖。
- **类型一致**：`close_slope(closes, period)`、`describe_regime(mode, adx, slope_short, slope_long, atr_pct)`、`_dump_live(self, mark, mode, inv, closes)`、`render_html(live: dict, equity: dict)`、`_read_live(path)`、`_latest_equity(jsonl_path)`、grid_live.json 字段名跨任务一致。
- **非破坏**：引擎写 grid_live.json 仅 `trend_aware=True` 路径；面板独立工具不连交易所、不参与交易。
- **执行分工**：每任务交 Codex(gpt-5.5, xhigh) 实现，Claude 审查 + 跑该任务 pytest 后再进下一个。
