# 有界趋势感知网格 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.
>
> **执行分工（用户要求）**：实现代码由 **Codex**（codex MCP，`workspace-write`）编写，Claude 全程盯审——把每个 Task 交给 Codex，拿回产出后审查 + 跑测试验证，不满意让它重做。见记忆 `codex-implements-claude-reviews`。

**Goal:** 给 Extended BTC 网格加"有界趋势感知"能力：强趋势/突破时停止亏损侧加仓、贴近强平确认平仓、交易所端 TPSL 兜底掉线，外加一个固定不追价的轻量有界区间；`--trend-aware` 显式启用，默认与现有实盘逐调用一致。

**Architecture:** 纯函数（门控/band/硬止损判定）与状态持久化独立成模块并单测；`GridEngine.run_once` 在 `--trend-aware` 开时走新分支（硬止损前置 → 二态 ADX 门控 → band 两态状态机 → 铺单/冻结）；交易所端整仓 reduce-only TPSL 随库存动态维护。顺带修 3 个现存 bug（`_cancel` 撤单前误删、`_handle_fills` 部分成交按整格翻单、`get_liquidation_info` 空仓/缺失 None 混淆）。

**Tech Stack:** Python 3.11、asyncio、pytest（`PYTHONPATH=. .venv/bin/python -m pytest`）、x10 SDK（`OrderType.TPSL` / `OrderTpslType.POSITION` / `mass_cancel`）。

**设计依据:** `docs/superpowers/specs/2026-07-27-有界趋势感知网格-design.md`（v3 定稿，三方一致）。

---

## 文件结构

**新建：**
- `grid/grid_state.py` — `GridState` dataclass + 原子读写 `data/grid_state.json`（band 边界 / frozen / blocked_side / halted）。单一职责：状态持久化。
- `grid/band.py` — 纯函数：`compute_band` / `is_out_of_band` / `blocked_side_for_breach`。单一职责：区间计算。
- `grid/risk.py` — 纯函数：`hard_stop_triggered` / `dist_to_liq_pct`。单一职责：硬止损判定。
- `tests/test_grid_state.py`、`tests/test_band.py`、`tests/test_risk.py`、`tests/test_trend_gate.py`、`tests/test_trend_aware_engine.py`、`tests/test_exchange_tpsl.py`。

**修改：**
- `grid/regime.py` — 新增 `trend_gate()`（二态，复用 `adx` + 迟滞）与 `drop_forming_candle()` helper。
- `adapters/base.py` / `adapters/extended_client.py` — `place_limit_order` 加 `reduce_only`；新增 `place_position_stop_loss` / `cancel_orders_where`（只撤非 reduce-only）/ `get_position_liquidation`（区分空仓/缺失）。
- `grid/grid_engine.py` — `GridConfig` 新字段；`run_once` trend-aware 分支；`_go_off_confirmed`；TPSL 维护；`_cancel`/`_handle_fills`/`_within_cap` 修正。
- `tools/run_grid.py` — 新 CLI 参数。
- `tools/grid_monitor.py` — 读 `grid_state.json` 报告新状态。

---

## Task 1: 状态持久化 `grid/grid_state.py`

**Files:**
- Create: `grid/grid_state.py`
- Test: `tests/test_grid_state.py`

- [ ] **Step 1: 写失败测试**

```python
# tests/test_grid_state.py
"""网格状态持久化测试：原子读写 + 缺失/损坏时的安全默认。"""
from __future__ import annotations

from grid.grid_state import GridState, load_state, save_state


def test_roundtrip(tmp_path) -> None:
    p = tmp_path / "grid_state.json"
    st = GridState(band_low=63000.0, band_high=68000.0, frozen=True,
                   blocked_side="BUY", halted=False)
    save_state(p, st)
    got = load_state(p)
    assert got == st


def test_missing_file_returns_none(tmp_path) -> None:
    assert load_state(tmp_path / "nope.json") is None


def test_corrupt_file_returns_none(tmp_path) -> None:
    p = tmp_path / "grid_state.json"
    p.write_text("{not json", encoding="utf-8")
    assert load_state(p) is None


def test_atomic_write_no_partial(tmp_path) -> None:
    # 覆盖写不应留下半截文件：写两次，第二次能完整读回
    p = tmp_path / "grid_state.json"
    save_state(p, GridState(1.0, 2.0, False, None, False))
    save_state(p, GridState(3.0, 4.0, True, "SELL", True))
    got = load_state(p)
    assert got == GridState(3.0, 4.0, True, "SELL", True)
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd /Users/captain/python/variational && PYTHONPATH=. .venv/bin/python -m pytest tests/test_grid_state.py -v`
Expected: FAIL（`ModuleNotFoundError: grid.grid_state`）

- [ ] **Step 3: 实现**

```python
# grid/grid_state.py
"""网格趋势感知状态的持久化（原子读写）。

launchd 会定期重启进程，band 边界与冻结/急停状态必须落盘，
否则重启后按现价重开网、绕过突破保护（见设计 v3 Component 2）。
"""
from __future__ import annotations

import json
import os
from dataclasses import asdict, dataclass
from pathlib import Path


@dataclass(frozen=True)
class GridState:
    band_low: float
    band_high: float
    frozen: bool
    blocked_side: str | None  # "BUY" / "SELL" / None
    halted: bool


def load_state(path: str | Path) -> GridState | None:
    """读回状态；文件缺失或损坏返回 None（调用方据此走 fail-closed）。"""
    p = Path(path)
    if not p.exists():
        return None
    try:
        d = json.loads(p.read_text(encoding="utf-8"))
        return GridState(**d)
    except Exception:  # noqa: BLE001  损坏当作无状态
        return None


def save_state(path: str | Path, state: GridState) -> None:
    """原子写：先写临时文件再 rename，避免半截文件。"""
    p = Path(path)
    p.parent.mkdir(parents=True, exist_ok=True)
    tmp = p.with_suffix(p.suffix + ".tmp")
    tmp.write_text(json.dumps(asdict(state), ensure_ascii=False), encoding="utf-8")
    os.replace(tmp, p)
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cd /Users/captain/python/variational && PYTHONPATH=. .venv/bin/python -m pytest tests/test_grid_state.py -v`
Expected: PASS（4 passed）

- [ ] **Step 5: 提交**

```bash
git add grid/grid_state.py tests/test_grid_state.py
git commit -m "feat(grid): 新增趋势感知状态持久化模块（原子读写）

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 2: band 纯函数 `grid/band.py`

**Files:**
- Create: `grid/band.py`
- Test: `tests/test_band.py`

- [ ] **Step 1: 写失败测试**

```python
# tests/test_band.py
"""有界区间 band 纯函数测试：最小半宽、越界判断、冻结方向推导。"""
from __future__ import annotations

from grid.band import blocked_side_for_breach, compute_band, is_out_of_band


def test_compute_band_symmetric() -> None:
    lo, hi = compute_band(price=65000.0, atr=500.0, k=1.75, min_half_frac=0.0)
    assert lo == 65000.0 - 1.75 * 500.0
    assert hi == 65000.0 + 1.75 * 500.0


def test_min_half_width_floor() -> None:
    # k*atr 只有 0.5%，min_half_frac=0.04（4%）应把半宽撑到 4%
    lo, hi = compute_band(price=100.0, atr=0.5, k=1.75, min_half_frac=0.04)
    assert hi - 100.0 == 4.0  # 撑到 4%
    assert 100.0 - lo == 4.0


def test_out_of_band() -> None:
    assert is_out_of_band(59.0, low=60.0, high=70.0) is True   # 跌破下界
    assert is_out_of_band(71.0, low=60.0, high=70.0) is True   # 涨破上界
    assert is_out_of_band(65.0, low=60.0, high=70.0) is False


def test_blocked_side_for_breach() -> None:
    # 跌破下界 → 亏损的是多头，冻结 BUY（停止继续买）
    assert blocked_side_for_breach(59.0, low=60.0, high=70.0) == "BUY"
    # 涨破上界 → 亏损的是空头，冻结 SELL
    assert blocked_side_for_breach(71.0, low=60.0, high=70.0) == "SELL"
    assert blocked_side_for_breach(65.0, low=60.0, high=70.0) is None
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd /Users/captain/python/variational && PYTHONPATH=. .venv/bin/python -m pytest tests/test_band.py -v`
Expected: FAIL（`ModuleNotFoundError: grid.band`）

- [ ] **Step 3: 实现**

```python
# grid/band.py
"""有界区间 band 纯函数（固定不追价，见设计 v3 Component 2）。"""
from __future__ import annotations


def compute_band(price: float, atr: float, k: float, min_half_frac: float) -> tuple[float, float]:
    """区间 = price ± max(k*atr, min_half_frac*price)。返回 (low, high)。

    min_half_frac 保证低波动时半宽不至于窄到只能挂一格。
    """
    half = max(k * atr, min_half_frac * price)
    return price - half, price + half


def is_out_of_band(price: float, low: float, high: float) -> bool:
    return price < low or price > high


def blocked_side_for_breach(price: float, low: float, high: float) -> str | None:
    """越界后应冻结（停止新增）的方向。跌破下界→冻结 BUY；涨破上界→冻结 SELL。"""
    if price < low:
        return "BUY"
    if price > high:
        return "SELL"
    return None
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cd /Users/captain/python/variational && PYTHONPATH=. .venv/bin/python -m pytest tests/test_band.py -v`
Expected: PASS（4 passed）

- [ ] **Step 5: 提交**

```bash
git add grid/band.py tests/test_band.py
git commit -m "feat(grid): 新增 band 纯函数（区间/越界/冻结方向）

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 3: 硬止损纯判定 `grid/risk.py`

**Files:**
- Create: `grid/risk.py`
- Test: `tests/test_risk.py`

- [ ] **Step 1: 写失败测试**

```python
# tests/test_risk.py
"""硬止损纯判定测试：距强平百分比 + 多空方向。"""
from __future__ import annotations

from grid.risk import dist_to_liq_pct, hard_stop_triggered


def test_dist_long() -> None:
    # 多头：liq 在下方，(mark-liq)/mark
    assert abs(dist_to_liq_pct(mark=100.0, liq=90.0, signed_size=1.0) - 0.10) < 1e-9


def test_dist_short() -> None:
    # 空头：liq 在上方，(liq-mark)/mark
    assert abs(dist_to_liq_pct(mark=100.0, liq=110.0, signed_size=-1.0) - 0.10) < 1e-9


def test_trigger_when_close() -> None:
    # 距强平 8% < 阈值 12% → 触发
    assert hard_stop_triggered(mark=100.0, liq=92.0, signed_size=1.0, stop_dist=0.12) is True


def test_no_trigger_when_far() -> None:
    # 距强平 20% > 12% → 不触发
    assert hard_stop_triggered(mark=100.0, liq=80.0, signed_size=1.0, stop_dist=0.12) is False


def test_flat_never_triggers() -> None:
    assert hard_stop_triggered(mark=100.0, liq=0.0, signed_size=0.0, stop_dist=0.12) is False
```

- [ ] **Step 2: 跑测试确认失败**

Run: `PYTHONPATH=. .venv/bin/python -m pytest tests/test_risk.py -v`
Expected: FAIL（`ModuleNotFoundError: grid.risk`）

- [ ] **Step 3: 实现**

```python
# grid/risk.py
"""硬止损纯判定（距强平价百分比，见设计 v3 Component 3）。"""
from __future__ import annotations


def dist_to_liq_pct(mark: float, liq: float, signed_size: float) -> float:
    """距强平价的相对距离（0~1）。空仓返回 inf（永不触发）。"""
    if signed_size == 0 or mark <= 0:
        return float("inf")
    if signed_size > 0:            # 多头，liq 在下
        return (mark - liq) / mark
    return (liq - mark) / mark     # 空头，liq 在上


def hard_stop_triggered(mark: float, liq: float, signed_size: float, stop_dist: float) -> bool:
    """距强平价 < stop_dist 即触发。空仓永不触发。"""
    return dist_to_liq_pct(mark, liq, signed_size) < stop_dist
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cd /Users/captain/python/variational && PYTHONPATH=. .venv/bin/python -m pytest tests/test_risk.py -v`
Expected: PASS（5 passed）

- [ ] **Step 5: 提交**

```bash
git add grid/risk.py tests/test_risk.py
git commit -m "feat(grid): 新增硬止损纯判定（距强平价百分比）

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 4: 二态趋势门控 `grid/regime.py`

**Files:**
- Modify: `grid/regime.py`（新增 `drop_forming_candle` 与 `trend_gate`，保留现有 `decide_mode` 不动）
- Test: `tests/test_trend_gate.py`

**契约:** `trend_gate` 复用现有 `GridMode`（NEUTRAL=可铺网格，OFF=风险关闭），**不新增枚举**。ADX 迟滞语义与 `decide_mode` 一致，但强制启用（阈值来自 config，不再是 999 禁用）。`drop_forming_candle` 用于剔除形成中的当前小时线。

- [ ] **Step 1: 写失败测试**

```python
# tests/test_trend_gate.py
"""二态趋势门控测试：震荡=NEUTRAL、强趋势=OFF、迟滞、只用已收盘K线。"""
from __future__ import annotations

import math

from grid.regime import GridMode, drop_forming_candle, trend_gate


def _synthetic():
    closes = [100 + math.sin(i / 3) * 2 for i in range(60)] + [100 + i * 2 for i in range(40)]
    highs = [c + 1 for c in closes]
    lows = [c - 1 for c in closes]
    return highs, lows, closes


def test_range_is_neutral() -> None:
    highs, lows, closes = _synthetic()
    m = trend_gate(highs[:55], lows[:55], closes[:55], adx_off=30, adx_resume=25,
                   prev_mode=GridMode.NEUTRAL)
    assert m is GridMode.NEUTRAL


def test_strong_trend_is_off() -> None:
    highs, lows, closes = _synthetic()
    m = trend_gate(highs, lows, closes, adx_off=30, adx_resume=25, prev_mode=GridMode.NEUTRAL)
    assert m is GridMode.OFF


def test_hysteresis_stays_off_until_resume() -> None:
    # 已 OFF：ADX 在 resume 与 off 之间应保持 OFF（迟滞）
    highs, lows, closes = _synthetic()
    # 用趋势段末尾，人为 prev_mode=OFF
    m = trend_gate(highs, lows, closes, adx_off=30, adx_resume=25, prev_mode=GridMode.OFF)
    assert m is GridMode.OFF


def test_drop_forming_candle() -> None:
    # 最后一根未收盘 → 去掉后长度 -1，且末元素变为倒数第二根
    xs = [1.0, 2.0, 3.0]
    assert drop_forming_candle(xs) == [1.0, 2.0]
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd /Users/captain/python/variational && PYTHONPATH=. .venv/bin/python -m pytest tests/test_trend_gate.py -v`
Expected: FAIL（`ImportError: cannot import name 'trend_gate'`）

- [ ] **Step 3: 实现（追加到 `grid/regime.py` 末尾，不改现有函数）**

```python
def drop_forming_candle(values: list[float]) -> list[float]:
    """去掉最后一根（形成中的当前 K 线），只用已收盘数据。"""
    return values[:-1] if values else values


def trend_gate(
    highs: list[float],
    lows: list[float],
    closes: list[float],
    *,
    adx_off: float,
    adx_resume: float,
    prev_mode: "GridMode | None" = None,
    adx_period: int = 14,
) -> GridMode:
    """二态趋势门控（设计 v3 Component 1）：强趋势=OFF（停止新增），否则 NEUTRAL。

    与 decide_mode 的区别：这里 ADX 熔断强制启用（阈值来自 config），
    调用方须先用 drop_forming_candle 传入已收盘 K 线。
    """
    a = adx(highs, lows, closes, adx_period)
    return decide_mode(adx_val=a[-1] if a else None,
                       adx_off=adx_off, adx_resume=adx_resume, prev_mode=prev_mode)
```

注：`connect()` 冷启动时 `prev_mode` 传 `GridMode.NEUTRAL`。`decide_mode` 已处理 `adx_val is None → OFF`（预热保守）。

- [ ] **Step 4: 跑测试确认通过**

Run: `cd /Users/captain/python/variational && PYTHONPATH=. .venv/bin/python -m pytest tests/test_trend_gate.py -v`
Expected: PASS（4 passed）

- [ ] **Step 5: 提交**

```bash
git add grid/regime.py tests/test_trend_gate.py
git commit -m "feat(grid): 新增二态趋势门控 trend_gate（复用ADX迟滞，只用已收盘K线）

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 5: 适配器扩展（reduce_only 限价单 + 只撤网格单 + 整仓 TPSL）

**Files:**
- Modify: `adapters/extended_client.py`
- Modify: `adapters/base.py`（抽象签名同步）
- Test: `tests/test_exchange_tpsl.py`

**契约（Codex 实现时对照 x10 SDK；真实交易所行为在 testnet 验证）:**

1. `place_limit_order(..., reduce_only: bool = False)` — 把 `reduce_only` 透传给 `create_order_object`（现固定 `False`，`extended_client.py:223`）。默认 False 保证旧行为不变。
2. `cancel_grid_orders(market) -> int` — 查 `get_open_orders`，**只撤 `reduce_only!=True` 且订单类型不是 `TPSL/CONDITIONAL` 的单**，保留 TPSL 兜底。返回撤单数。**绝不用 `mass_cancel(cancel_all=True)`**（会连 TPSL 一起撤，见设计 v3 Component 3/4）。
3. `place_position_stop_loss(market, signed_size, trigger_price)` — 挂整仓 reduce-only 止损：`orders.place_order(..., type=OrderType.TPSL, tp_sl_type=OrderTpslType.POSITION, stop_loss=OrderTpslTriggerParam(trigger_price=..., price_type=OrderTriggerPriceType.MARK, ...))`。多头仓 → 止损在下方触发平多；空头相反。
4. `cancel_tpsl(market)` — 撤掉本引擎挂的整仓 TPSL（仓位确认归零后调用）。

**说明:** `get_position_liquidation` 不新增——引擎侧用现有 `get_position`（拿 `signed_size`）+ `get_liquidation_info` 组合判断"空仓 vs 有仓拿不到"，见 Task 6d。

- [ ] **Step 1: 写失败测试（用 Fake 桩验证引擎侧契约，不真连交易所）**

```python
# tests/test_exchange_tpsl.py
"""适配器扩展契约测试：reduce_only 透传、只撤网格单保留TPSL。

真实 SDK 下单/撤单的交易所行为在 testnet 单独验证（见计划 Task 9 说明）。
本测试只锁定"过滤逻辑"这一可纯测的部分。
"""
from __future__ import annotations

from types import SimpleNamespace

from adapters.extended_client import filter_grid_orders  # 纯函数：从开放单里挑出该撤的网格单


def _o(oid, reduce_only=False, otype="LIMIT"):
    return SimpleNamespace(id=oid, reduce_only=reduce_only, type=otype)


def test_filter_keeps_tpsl_and_reduce_only() -> None:
    orders = [_o("g1"), _o("g2"), _o("tp", otype="TPSL"),
              _o("ro", reduce_only=True)]
    to_cancel = filter_grid_orders(orders)
    ids = {getattr(o, "id") for o in to_cancel}
    assert ids == {"g1", "g2"}  # 只撤普通网格单，保留 TPSL 与 reduce_only
```

- [ ] **Step 2: 跑测试确认失败**

Run: `PYTHONPATH=. .venv/bin/python -m pytest tests/test_exchange_tpsl.py -v`
Expected: FAIL（`ImportError: cannot import name 'filter_grid_orders'`）

- [ ] **Step 3: 实现**

在 `adapters/extended_client.py` 新增纯函数（供 `cancel_grid_orders` 内部使用、可单测）：

```python
def filter_grid_orders(open_orders: list) -> list:
    """从开放订单里挑出"普通网格单"（该撤的），保留 TPSL / reduce_only 兜底单。"""
    out = []
    for o in open_orders:
        if bool(getattr(o, "reduce_only", False)):
            continue
        if str(getattr(o, "type", "") or "").upper() in ("TPSL", "CONDITIONAL"):
            continue
        out.append(o)
    return out
```

再实现 `place_limit_order` 的 `reduce_only` 透传、`cancel_grid_orders`（用 `filter_grid_orders` + 逐单 `cancel_order`）、`place_position_stop_loss`、`cancel_tpsl`。`adapters/base.py` 同步抽象签名（给默认实现或 `@abstractmethod` 视需要）。

- [ ] **Step 4: 跑测试确认通过**

Run: `cd /Users/captain/python/variational && PYTHONPATH=. .venv/bin/python -m pytest tests/test_exchange_tpsl.py -v`
Expected: PASS（1 passed）

- [ ] **Step 5: 提交**

```bash
git add adapters/extended_client.py adapters/base.py tests/test_exchange_tpsl.py
git commit -m "feat(adapters): reduce_only 限价单 + 只撤网格单(保留TPSL) + 整仓TPSL

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 6a: 修 bug — `_cancel` 撤单确认后再删记录

**Files:**
- Modify: `grid/grid_engine.py:225-235`（`_cancel`）
- Test: `tests/test_grid_engine.py`（追加）

**Bug:** 现 `_cancel` 先 `self._orders.pop(level)` 再调 `cancel_order`；撤单抛异常时记录已删，交易所上留孤儿风险单而引擎已遗忘。

- [ ] **Step 1: 追加失败测试**

```python
def test_cancel_keeps_record_on_failure() -> None:
    """撤单失败时不能把订单记录丢掉（否则留孤儿单）。"""
    class FailExt(FakeExt):
        async def cancel_order(self, oid):
            raise RuntimeError("network")
    ext = FailExt()
    eng = _engine(ext, {560: {"id": "o1", "side": Side.SELL}})
    asyncio.run(eng._cancel(560, why="test"))
    assert 560 in eng._orders  # 撤单失败 → 记录保留，下轮重试
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd /Users/captain/python/variational && PYTHONPATH=. .venv/bin/python -m pytest tests/test_grid_engine.py::test_cancel_keeps_record_on_failure -v`
Expected: FAIL（记录被误删，`560 not in eng._orders`）

- [ ] **Step 3: 实现（改 `_cancel`）**

```python
    async def _cancel(self, level: int, why: str = "") -> None:
        rec = self._orders.get(level)
        if not rec:
            return
        if self.config.dry_run:
            logger.info("[dry_run] %s：撤 格%d", why, level)
            self._orders.pop(level, None)
            return
        try:
            await self.ext.cancel_order(rec["id"])
            self._orders.pop(level, None)  # 撤单成功后才删记录
        except Exception as exc:  # noqa: BLE001
            logger.warning("撤单失败 格%d：%s（保留记录下轮重试）", level, exc)
```

- [ ] **Step 4: 跑测试确认通过（并确保旧测试不回归）**

Run: `cd /Users/captain/python/variational && PYTHONPATH=. .venv/bin/python -m pytest tests/test_grid_engine.py -v`
Expected: PASS（含新测试，旧测试不变）

- [ ] **Step 5: 提交**

```bash
git add grid/grid_engine.py tests/test_grid_engine.py
git commit -m "fix(grid): _cancel 撤单成功后才删记录，避免撤单失败留孤儿单

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 6b: 修 bug — `_within_cap` 按真实持仓 + 挂单剩余量算最坏仓位

**Files:**
- Modify: `grid/grid_engine.py:191-199`（`_within_cap`）
- Test: `tests/test_grid_engine.py`（追加）

**Bug:** 现 `_within_cap` 按"订单数 × 名义"估算，不看部分成交/实际持仓，最坏仓位会低估。契约：入参增加"当前该侧挂单的剩余名义合计"，用真实持仓 + 剩余名义算满仓后是否超上限。

- [ ] **Step 1: 追加失败测试**

```python
def test_within_cap_uses_real_inventory() -> None:
    """已有多头库存接近上限时，不应再允许新增买单。"""
    ext = FakeExt()
    eng = GridEngine(ext, GridConfig(dry_run=False, unit_usd=200, max_inventory_usd=1600))
    # 已持多头 $1500，再挂 1 张 $200 买单会到 $1700 > 上限
    assert eng._within_cap(Side.BUY, inv_usd=1500.0) is False
    # 持多头 $1000，挂 $200 到 $1200 < 上限 → 允许
    assert eng._within_cap(Side.BUY, inv_usd=1000.0) is True
```

- [ ] **Step 2: 跑测试确认（现实现可能已巧合通过；若通过则补一个能暴露"按订单数估算"偏差的用例）**

Run: `cd /Users/captain/python/variational && PYTHONPATH=. .venv/bin/python -m pytest tests/test_grid_engine.py::test_within_cap_uses_real_inventory -v`
Expected: 先确认现行为，再据实调整实现使语义为"真实持仓 + 已挂该侧剩余名义 + 新单 ≤ 上限"

- [ ] **Step 3: 实现（改 `_within_cap`，按真实持仓 + 该侧已挂剩余名义）**

保持既有签名 `_within_cap(side, inv_usd)`，但内部：该侧库存基数 = `inv_usd`（多头正、空头取 `-inv_usd`）；加上该侧已挂单剩余名义（`unit_usd × 该侧挂单数`，若引擎记录了每单剩余量则用剩余量）；再加 1 个 `unit_usd`；判 `≤ max_inventory_usd`。

- [ ] **Step 4: 跑测试确认通过**

Run: `cd /Users/captain/python/variational && PYTHONPATH=. .venv/bin/python -m pytest tests/test_grid_engine.py -v`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add grid/grid_engine.py tests/test_grid_engine.py
git commit -m "fix(grid): _within_cap 按真实持仓+挂单剩余名义算最坏仓位

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 6c: `_handle_fills` — 部分成交按 filled_qty + 冻结侧不重挂

**Files:**
- Modify: `grid/grid_engine.py:138-167`（`_handle_fills`）
- Test: `tests/test_grid_engine.py`（追加）

**契约:** ① 翻单/退出数量按实际 `filled_qty`（现按整格 `unit_usd`，部分成交会错，`grid_engine.py:158`）。② `_handle_fills` 接受"被冻结方向 `blocked_side`"，冻结侧的 EXPIRED/成交**不重新挂该方向新单**（趋势/突破时不再往亏损侧加仓）。默认 `blocked_side=None` 时行为与现状一致。

- [ ] **Step 1: 追加失败测试**

```python
def test_frozen_side_not_replenished() -> None:
    """冻结 BUY 时，买单过期不重挂买单。"""
    ext = FakeExt(open_orders=[], history=[_hist("o1", "EXPIRED")])
    eng = _engine(ext, {558: {"id": "o1", "side": Side.BUY}})
    asyncio.run(eng._handle_fills(0.0, blocked_side="BUY"))
    assert ext.placed == []  # 冻结侧不重挂


def test_partial_fill_uses_filled_qty() -> None:
    """部分成交翻单数量按 filled_qty，不按整格。"""
    ext = FakeExt(open_orders=[], history=[_hist("o1", "FILLED", filled_qty=0.0002)])
    eng = _engine(ext, {558: {"id": "o1", "side": Side.BUY}})
    asyncio.run(eng._handle_fills(0.0))
    assert len(ext.placed) == 1
    # 退出单数量应对应 0.0002 而非整格 unit_usd/price
```

- [ ] **Step 2: 跑测试确认失败**

Run: `PYTHONPATH=. .venv/bin/python -m pytest tests/test_grid_engine.py::test_frozen_side_not_replenished tests/test_grid_engine.py::test_partial_fill_uses_filled_qty -v`
Expected: FAIL（`_handle_fills` 未接受 `blocked_side` 参数 / 数量按整格）

- [ ] **Step 3: 实现**

`_handle_fills(self, inv_usd, blocked_side=None)`：翻出的退出单方向若等于 `blocked_side` 则跳过；退出/翻单数量用 `filled_qty`（无则退回 `unit_usd/price`）。`FakeExt.place_limit_order` 已记录 side，可扩展记录 amount 便于断言。

- [ ] **Step 4: 跑测试确认通过**

Run: `cd /Users/captain/python/variational && PYTHONPATH=. .venv/bin/python -m pytest tests/test_grid_engine.py -v`
Expected: PASS（含旧 6 个翻单测试不回归）

- [ ] **Step 5: 提交**

```bash
git add grid/grid_engine.py tests/test_grid_engine.py
git commit -m "feat(grid): _handle_fills 按filled_qty翻单 + 冻结侧不重挂

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 6d: 硬止损前置 + `_go_off_confirmed`（确认平仓链）+ None 区分

**Files:**
- Modify: `grid/grid_engine.py`（新增 `_check_hard_stop` / `_go_off_confirmed`）
- Test: `tests/test_trend_aware_engine.py`（新建）

**契约（设计 v3 Component 3）:**
- `_check_hard_stop()` 每轮**最先**调用（在拉 K 线之前）。先 `get_position` 拿 `signed_size`；`signed_size==0` → 不检查（空仓时 `get_liquidation_info` 返回 None 是正常，**不 fail-safe**）。`signed_size!=0` 且 `get_liquidation_info` 返回 None → fail-safe（冻结新增，记日志）。拿到 (mark, liq) → `hard_stop_triggered(mark, liq, signed_size, stop_dist)` 为真 → `_go_off_confirmed`。
- `_go_off_confirmed(inv)`：① 持久化 `halted=true`；② `cancel_grid_orders`（**只撤网格单，保留 TPSL**）；③ 读仓位 → `market_order(reduce_only=True)`；④ 重读仓位，未平按剩余量重试（上限 N 次，逐级放宽滑点）；⑤ 连续两次读仓位为零才算成功，然后 `cancel_tpsl`；⑥ `halted` 不自动恢复。

- [ ] **Step 1: 写失败测试**

```python
# tests/test_trend_aware_engine.py
"""trend-aware 引擎集成测试：硬止损确认链、空仓/缺失区分、band 状态机。"""
from __future__ import annotations

import asyncio
from decimal import Decimal
from types import SimpleNamespace

from adapters.base import Position, Side
from grid.grid_engine import GridConfig, GridEngine


class StopExt:
    """桩：可编排持仓序列与清算价，记录平仓调用。"""
    def __init__(self, positions, liq=None):
        self._positions = list(positions)  # 依次返回的 signed_size
        self._liq = liq                    # (mark, liq) 或 None
        self.market_orders = []
        self.grid_cancelled = 0
        self.tpsl_cancelled = 0

    async def get_position(self, market):
        size = self._positions[0] if len(self._positions) == 1 else self._positions.pop(0)
        return Position(market=market, signed_size=Decimal(str(size)))

    async def get_liquidation_info(self, market):
        return self._liq

    async def cancel_grid_orders(self, market):
        self.grid_cancelled += 1
        return 0

    async def cancel_tpsl(self, market):
        self.tpsl_cancelled += 1

    async def market_order(self, market, side, amount, *, reduce_only=False):
        self.market_orders.append((side, float(amount), reduce_only))


def _eng(ext):
    return GridEngine(ext, GridConfig(dry_run=False, trend_aware=True, hard_stop_dist=0.12))


def test_flat_position_no_failsafe() -> None:
    """空仓 + 清算价 None → 正常，不触发硬止损。"""
    ext = StopExt(positions=[0.0], liq=None)
    eng = _eng(ext)
    assert asyncio.run(eng._check_hard_stop()) is False


def test_hard_stop_confirms_flat() -> None:
    """距强平 8% < 12% → 平仓；第一次读仍有仓、第二次读为零才算成功。"""
    # 持多头 0.02，mark 100 liq 93 → 距 7% 触发；平仓后仓位序列归零
    ext = StopExt(positions=[0.02, 0.02, 0.0, 0.0], liq=(100.0, 93.0))
    eng = _eng(ext)
    triggered = asyncio.run(eng._check_hard_stop())
    assert triggered is True
    assert ext.grid_cancelled >= 1                 # 撤了网格单
    assert any(ro for (_, _, ro) in ext.market_orders)  # 用了 reduce_only 平仓
    assert ext.tpsl_cancelled >= 1                 # 归零后才撤 TPSL
```

- [ ] **Step 2: 跑测试确认失败**

Run: `PYTHONPATH=. .venv/bin/python -m pytest tests/test_trend_aware_engine.py -v`
Expected: FAIL（`_check_hard_stop` 未定义）

- [ ] **Step 3: 实现** `_check_hard_stop` 与 `_go_off_confirmed`（按上文契约；`hard_stop_triggered` 来自 `grid.risk`）。旧 `_go_off` 保留给非 trend-aware 路径不动。

- [ ] **Step 4: 跑测试确认通过**

Run: `cd /Users/captain/python/variational && PYTHONPATH=. .venv/bin/python -m pytest tests/test_trend_aware_engine.py -v`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add grid/grid_engine.py tests/test_trend_aware_engine.py
git commit -m "feat(grid): 硬止损前置+确认平仓链(_go_off_confirmed)+空仓/缺失None区分

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 6e: `run_once` trend-aware 分支 + band 两态状态机

**Files:**
- Modify: `grid/grid_engine.py`（`GridConfig` 新字段、`run_once` 分支、`_maintain_ladder` band 约束、band 状态推进 `_advance_band`）
- Test: `tests/test_trend_aware_engine.py`（追加）

**`GridConfig` 新字段（均默认值，非破坏）:**
```python
    trend_aware: bool = False
    band_k: float = 1.75
    min_half_frac: float = 0.04        # band 最小半宽（占价格比），保证每侧≥~4格
    hard_stop_dist: float = 0.12
    recenter_bars: int = 3             # 连续几根 NEUTRAL 才允许重建 band
    state_path: str = "data/grid_state.json"
    exchange_tpsl: bool = True
```

**契约:**
- `run_once` 开头：`if self.config.trend_aware:` 走新流程，否则**原封不动**走现有流程（回归测试锁定）。
- 新流程顺序：`_check_hard_stop()`（触发则本轮 return）→ 若 `halted` return → 拉 K 线 `drop_forming_candle` → `trend_gate` → `_advance_band(mark, mode)` → 动作。
- `_advance_band`：无状态/ACTIVE 且 `is_out_of_band(mark)` → 转 FROZEN，`blocked_side=blocked_side_for_breach(...)`，`cancel_grid_orders` 冻结侧、`save_state`；FROZEN 且 `仓位==0 且 无非reduce_only挂单 且 连续 recenter_bars 根 NEUTRAL` → `compute_band` 重建、清 `blocked_side`、转 ACTIVE、`save_state`。
- 动作：`mode is OFF` → 视为冻结双侧新增（`blocked_side` 效果=双向），只 `_handle_fills(inv_usd, blocked_side=...)`，不 `_maintain_ladder`；`NEUTRAL + ACTIVE` → `_handle_fills` + `_maintain_ladder`，铺单范围 = `[center±n] ∩ band`（band 外不挂）、跳过 `blocked_side`。
- `connect()`：`load_state`；文件缺失/损坏且**有仓或有挂单** → fail-closed（不新建 band、进入 FROZEN 等人工介入）；空仓无挂单 → 允许首轮新建。

- [ ] **Step 1: 追加失败测试**

```python
def test_breach_freezes_and_does_not_recenter(tmp_path) -> None:
    """价格跌破下界 → FROZEN + blocked_side=BUY + 撤买单；band 不追价。"""
    ext = StopExt(positions=[0.01], liq=(100.0, 80.0))  # 距强平20%不触发硬止损
    cfg = GridConfig(dry_run=False, trend_aware=True, state_path=str(tmp_path / "s.json"))
    eng = GridEngine(ext, cfg)
    # 预置 ACTIVE band [95,105]，喂 mark=90（跌破下界）
    ... # 设置 eng 内部 band 状态为 ACTIVE(95,105)
    asyncio.run(eng._advance_band(mark=90.0, mode=GridMode.OFF))
    from grid.grid_state import load_state
    st = load_state(cfg.state_path)
    assert st.frozen is True and st.blocked_side == "BUY"
    assert st.band_low == 95.0 and st.band_high == 105.0  # 不追价重心
```

- [ ] **Step 2: 跑测试确认失败** → `_advance_band` 未定义
- [ ] **Step 3: 实现** `_advance_band`、`run_once` 分支、`_maintain_ladder` 加 band/blocked_side 约束、`connect` fail-closed。
- [ ] **Step 4: 跑测试确认通过**

Run: `PYTHONPATH=. .venv/bin/python -m pytest tests/test_trend_aware_engine.py -v`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add grid/grid_engine.py tests/test_trend_aware_engine.py
git commit -m "feat(grid): run_once trend-aware 分支 + band 两态状态机(不追价+持久化)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 6f: 交易所端整仓 TPSL 动态维护

**Files:**
- Modify: `grid/grid_engine.py`（`_maintain_tpsl`）
- Test: `tests/test_exchange_tpsl.py`（追加，用桩）

**契约（设计 v3 Component 4）:** `exchange_tpsl=True` 且有风险仓时，每轮（或每次风险仓增加后）计算止损触发价（按距强平 `hard_stop_dist` 或更早）并 `place_position_stop_loss` 挂/更新整仓 reduce-only TPSL，确认其处于 `UNTRIGGERED`。**TPSL 缺失/更新失败/未确认 UNTRIGGERED → 禁止继续新增风险仓**（`_maintain_ladder` 跳过新增，仅保留退出）。仓位归零后由 `_go_off_confirmed`/正常流程 `cancel_tpsl`。

- [ ] **Step 1: 追加失败测试**

```python
def test_tpsl_blocks_new_risk_when_unconfirmed() -> None:
    """TPSL 挂单失败/未确认 → 本轮禁止新增风险仓。"""
    class NoTpslExt(StopExt):
        async def place_position_stop_loss(self, market, signed_size, trigger_price):
            raise RuntimeError("tpsl rejected")
    ext = NoTpslExt(positions=[0.01], liq=(100.0, 80.0))
    cfg = GridConfig(dry_run=False, trend_aware=True, exchange_tpsl=True)
    eng = GridEngine(ext, cfg)
    ok = asyncio.run(eng._maintain_tpsl(mark=100.0, signed_size=0.01))
    assert ok is False   # 未确认 → 上层据此禁止新增
```

- [ ] **Step 2: 跑测试确认失败** → `_maintain_tpsl` 未定义
- [ ] **Step 3: 实现** `_maintain_tpsl(mark, signed_size) -> bool`（成功确认返回 True，否则 False；`run_once`/`_maintain_ladder` 用返回值决定是否允许新增）。
- [ ] **Step 4: 跑测试确认通过**

Run: `cd /Users/captain/python/variational && PYTHONPATH=. .venv/bin/python -m pytest tests/test_exchange_tpsl.py -v`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add grid/grid_engine.py tests/test_exchange_tpsl.py
git commit -m "feat(grid): 交易所端整仓TPSL动态维护+未确认时禁止新增风险仓

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 7: CLI 参数 `tools/run_grid.py`

**Files:**
- Modify: `tools/run_grid.py`

- [ ] **Step 1: 加参数并透传到 GridConfig**

```python
    p.add_argument("--trend-aware", action="store_true", help="启用有界趋势感知(默认关闭=现有行为)")
    p.add_argument("--band-k", type=float, default=1.75, help="band 半宽=k×ATR")
    p.add_argument("--hard-stop-dist", type=float, default=0.12, help="距强平价触发硬止损的比例")
```
`GridConfig(..., trend_aware=args.trend_aware, band_k=args.band_k, hard_stop_dist=args.hard_stop_dist)`；启动打印行注明是否 trend-aware。

- [ ] **Step 2: 冒烟——dry_run 起一轮不报错**

Run: `PYTHONPATH=. .venv/bin/python -m tools.run_grid --trend-aware --interval 1 &  sleep 8; kill %1`
Expected: 日志出现 trend-aware 启动行、无异常回溯

- [ ] **Step 3: 提交**

```bash
git add tools/run_grid.py
git commit -m "feat(grid): run_grid 增加 --trend-aware/--band-k/--hard-stop-dist

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 8: 监控适配 `tools/grid_monitor.py`

**Files:**
- Modify: `tools/grid_monitor.py`
- Test: `tests/test_monitor.py`（追加，如现有测试覆盖 snapshot 字段）

**契约:** snapshot 增加从 `data/grid_state.json`（`grid.grid_state.load_state`）读出的 `frozen / blocked_side / halted / band_low / band_high`，以及"距强平%"（由 `get_liquidation_info` + `grid.risk.dist_to_liq_pct`）。`--report` 增打印这些字段。**不重新实现分类器**；状态缺失时字段填 None，不报错。

- [ ] **Step 1: 追加/调整测试**（若 `test_monitor.py` 断言了 snapshot 结构，补新字段的存在性断言；否则加一个 `load_state` 为 None 时 snapshot 不抛错的用例）
- [ ] **Step 2: 跑测试确认失败**

Run: `PYTHONPATH=. .venv/bin/python -m pytest tests/test_monitor.py -v`

- [ ] **Step 3: 实现**（读 `grid_state.json` + 距强平%；`--report` 增行）
- [ ] **Step 4: 跑测试确认通过**

Run: `cd /Users/captain/python/variational && PYTHONPATH=. .venv/bin/python -m pytest tests/test_monitor.py -v`

- [ ] **Step 5: 提交**

```bash
git add tools/grid_monitor.py tests/test_monitor.py
git commit -m "feat(monitor): 报告 band/frozen/halted/距强平%（读grid_state）

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 9: 非破坏回归 + 全量测试 + testnet 验证清单

**Files:**
- Test: `tests/test_trend_aware_engine.py`（追加回归用例）

- [ ] **Step 1: 加"trend_aware=False 逐调用与现状一致"回归测试**

```python
def test_default_path_unchanged() -> None:
    """trend_aware=False 时 run_once 不触碰任何新逻辑：不查清算价、不读状态文件。"""
    # 用一个会在被调用新方法时抛错的桩，确保默认路径不调用它们
    class GuardExt(FakeExt):
        async def get_liquidation_info(self, market):
            raise AssertionError("默认路径不应查清算价")
    # 断言：GridConfig(trend_aware=False) 下走原 decide_mode 路径，不调用 get_liquidation_info
```
（实现细节：默认路径完全不进入 `_check_hard_stop`；如需 mock K 线，复用现有 `run_once` 测试的桩方式。）

- [ ] **Step 2: 全量测试**

Run: `cd /Users/captain/python/variational && PYTHONPATH=. .venv/bin/python -m pytest tests/ -v`
Expected: 全绿（现有 test_grid_engine/test_regime/test_monitor 等不回归 + 新增全部通过）

- [ ] **Step 3: 提交**

```bash
git add tests/test_trend_aware_engine.py
git commit -m "test(grid): trend_aware=False 非破坏回归 + 全量绿

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

- [ ] **Step 4: testnet 手动验证清单（上实盘前，Claude 盯 Codex 逐条核）**

  1. `place_position_stop_loss` 在 testnet 真能挂出整仓 reduce-only TPSL，`type=TPSL`、`UNTRIGGERED`。
  2. `cancel_grid_orders` 撤网格单时 **TPSL 仍在**（不被误撤）。
  3. dead-man/`mass_cancel(cancel_all=True)` 与 TPSL 的共存行为（若启用 dead-man，确认是否连 TPSL 一起撤；未验证前不启用、不宣称独立保护）。
  4. 硬止损触发时 `_go_off_confirmed` 确实把仓位平到零（含 IOC 部分成交重试）。
  5. 进程重启后从 `grid_state.json` 恢复 FROZEN/halted，不按现价重开网。

- [ ] **Step 5: 上线切换（人工，非自动）**

  切 live 前先 `dry_run --trend-aware` 观察数轮日志；确认 band/门控/状态文件行为符合预期后，再改 launchd plist 加 `--trend-aware` 并重启服务。切换前撤旧单或让 `connect()` 接管。

---

## Self-Review（计划完成后自查）

- **Spec 覆盖**：C1 二态门控→T4；C2 band+持久化→T1/T2/T6e；C3 硬止损确认链→T3/T6d；C4 交易所TPSL→T5/T6f；C5 订单安全→T5/T6a/T6b/T6c；C6 监控→T8；C7 配置/CLI/回归→T6e/T7/T9。全部有对应 Task。
- **类型一致**：`GridState(band_low, band_high, frozen, blocked_side, halted)`、`trend_gate/decide_mode` 用 `GridMode`、`hard_stop_triggered(mark, liq, signed_size, stop_dist)`、`compute_band(price, atr, k, min_half_frac)`、`blocked_side_for_breach`、`filter_grid_orders`、`_check_hard_stop/_go_off_confirmed/_advance_band/_maintain_tpsl/_handle_fills(..., blocked_side)` 跨任务一致。
- **执行分工**：每个 Task 交 Codex 实现，Claude 审查 + 跑该 Task 的 pytest 验证后再进下一个。
