# Lighter 交易适配器 实施计划（做市项目第一期）

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让 Lighter 适配器能安全地下单、查单、撤单，并统一现有适配器的方法契约。交付物可独立验证：不接引擎，用真实账户小额挂一单再撤掉即可证明正确。

**Architecture:** 先统一 `ExchangeAdapter` 的方法契约（当前 Extended 与计划中的 Lighter 签名不一致），再补齐 Lighter 的交易能力。Lighter 下单响应不返回订单号，因此需要一个「客户端订单号 → 交易所订单号」的映射层，靠查询接口建立。

**Tech Stack:** Python 3.11、`lighter_sdk` 1.1.2、pytest。

---

## 背景（实施者必读）

- 设计文档：`docs/superpowers/specs/2026-08-17-Lighter做市刷量-design.md`
- 本计划是第一期。**第二期（引擎解耦 + 做市入口 + 对冲改造）不在本计划范围内。**
- 跑测试：`.venv/bin/python -m pytest tests/ -q`，**当前基线 505 passed**
- 中文注释与文档字符串，英文标识符
- **三个实盘进程正在跑真钱**：Extended 网格（PID 56652）、Lighter 对冲（PID 81888）、面板。**不得 kill 进程、不得跑 launchctl**

### 为什么会有这一期

本项目的第一版计划经两位独立审查者审查后判定不可实施，发现 7 个 P0，其中 3 个会打坏正在运行的实盘系统。根因是对现有代码核对不足。**本计划的所有事实均已逐条实测**，下面列出的每个签名、字段、行号都经过验证。

---

## 已实测确认的事实（直接用，不要重新摸索）

### SDK 版本与安装

```
包名：lighter_sdk        版本：1.1.2
commit：da322db2ca4fb3fac56b463d0ade4e54960e32be
```

**必须锁 commit 安装**，不能装默认分支——签名器与返回模型可能随分支变化，而单测全部绕过真实包：

```
lighter_sdk @ git+https://github.com/elliottech/lighter-python.git@da322db2ca4fb3fac56b463d0ade4e54960e32be
```

### SignerClient 签名（实测）

```python
lighter.SignerClient(url, account_index, api_private_keys: Dict[int, str],
                     nonce_management_type=..., chain_id=None)

create_order(market_index, client_order_index, base_amount, price, is_ask,
             order_type, time_in_force, reduce_only=False, trigger_price=0,
             order_expiry=-1, *, nonce=-1, api_key_index=255) -> (tx, resp, err)

create_market_order(market_index, client_order_index, base_amount,
                    avg_execution_price, is_ask, reduce_only=False,
                    *, nonce=-1, api_key_index=255) -> (tx, resp, err)

cancel_order(market_index, order_index, *, nonce=-1, api_key_index=255) -> (tx, resp, err)

cancel_all_orders(time_in_force, timestamp_ms, cancel_all_market_index=255, ...)
create_auth_token_with_expiry(deadline=-1, *, timestamp=None, api_key_index=255)
```

常量：`ORDER_TYPE_LIMIT=0`、`ORDER_TYPE_MARKET=1`、`ORDER_TIME_IN_FORCE_IMMEDIATE_OR_CANCEL=0`、`GOOD_TILL_TIME=1`、`POST_ONLY=2`、`DEFAULT_API_KEY_INDEX=255`。

### ⚠️ 下单响应不返回订单号

`RespSendTx` 的全部字段（实测）：

```
code, message, tx_hash, predicted_execution_time_ms, volume_quota_remaining, additional_properties
```

**没有 order_index。** 而 `cancel_order` 必须传 `order_index`。因此必须通过查询接口建立映射。

`Order` 模型有这些字段（实测，节选）：

```
order_index          ← 撤单需要它
client_order_index   ← 下单时我们自己设的，用它做关联
filled_base_amount   ← 部分成交量
remaining_base_amount
status, is_ask, price, initial_base_amount
```

查询方法：`lighter.OrderApi.account_active_orders`，**需要鉴权**（实测无 token 返回 `{"code":20001,"message":"...auth query param and Authorization header are empty"}`），token 由 `create_auth_token_with_expiry()` 生成。

### BTC 市场参数（实测）

```
market_id = 1        min_base_amount = 0.00020     min_quote_amount = 10.0
size_decimals = 5    price_decimals = 1            taker_fee = maker_fee = 0
```

换算：`base_amount = BTC × 100000`（0.00317 → `317`）、`price = USD × 10`（63400.0 → `634000`）。均为整数。

### ⚠️ 现有 cancel_order 契约不一致

```python
# adapters/extended_client.py:344
async def cancel_order(self, order_id) -> None:      # 一个参数

# grid/grid_engine.py:1808
await self.ext.cancel_order(rec["id"])                # 一个参数
```

Lighter 撤单需要 `market_index` + `order_index` 两个信息。**必须先统一契约**（Task 1），否则任何跨适配器的代码都会踩 `TypeError`，而假对象若照抄错误签名，测试会全绿、生产必炸。

### 引擎实际调用的适配器方法（实测计数）

```
8 get_position          5 get_open_orders       2 market_order
2 get_orders_history    2 cancel_grid_orders    1 place_position_stop_loss
1 place_limit_order     1 get_liquidation_info  1 connect
1 cancel_tpsl           1 cancel_order
```

`LighterClient` 当前只有：`connect`、`get_position`、`get_market_price`、`get_liquidation_info`、`get_collateral`、`market_order`（桩）、`close_position`。**缺 7 个。**

---

## 文件结构

| 文件 | 职责 |
|---|---|
| `adapters/lighter_scale.py` | 纯函数：小数 ↔ 整数换算，含最小下单量校验 |
| `adapters/order_ref.py` | 统一的下单返回值 `OrderRef`，屏蔽各所差异 |
| `adapters/base.py` | 修改：统一 `cancel_order(market, order_id)` 契约 |
| `adapters/extended_client.py` | 修改：`cancel_order` 加 market 参数（保持行为不变） |
| `adapters/lighter_client.py` | 修改：交易开关 + 7 个交易方法 + 订单号映射 |
| `grid/grid_engine.py` | 修改：仅 1 行，`cancel_order` 调用加 market 参数 |
| `tests/test_adapter_contract.py` | 契约测试：用 inspect.signature 断言两个适配器签名一致 |

---

## Task 1: 适配器契约统一

**这一个任务同时拆掉三颗雷**：签名不一致导致的 `TypeError`、假对象照抄错误签名、跨适配器代码无法复用。

**Files:**
- Modify: `adapters/base.py`、`adapters/extended_client.py`、`grid/grid_engine.py:1808`
- Create: `tests/test_adapter_contract.py`

- [ ] **Step 1: 写失败测试**

创建 `tests/test_adapter_contract.py`：

```python
"""适配器契约测试。

存在理由：Extended 的 cancel_order 是一个参数，而 Lighter 撤单需要
market + order_index 两个信息。若两边签名不一致，跨适配器的代码会踩
TypeError；更糟的是假对象若照抄错误签名，测试会全绿而生产必炸。
本文件用 inspect.signature 把契约钉死。
"""

from __future__ import annotations

import inspect

import pytest

from adapters.base import ExchangeAdapter
from adapters.extended_client import ExtendedClient
from adapters.lighter_client import LighterClient

_ADAPTERS = [ExtendedClient, LighterClient]


@pytest.mark.parametrize("cls", _ADAPTERS)
def test_cancel_order_takes_market_and_order_id(cls):
    params = list(inspect.signature(cls.cancel_order).parameters)
    assert params[:3] == ["self", "market", "order_id"], (
        f"{cls.__name__}.cancel_order 签名不符合契约，实际 {params}"
    )


@pytest.mark.parametrize("cls", _ADAPTERS)
def test_market_order_signature_matches_base(cls):
    base = list(inspect.signature(ExchangeAdapter.market_order).parameters)
    got = list(inspect.signature(cls.market_order).parameters)
    assert got == base, f"{cls.__name__}.market_order 与基类不一致：{got} != {base}"


@pytest.mark.parametrize("cls", _ADAPTERS)
def test_get_position_signature_matches_base(cls):
    base = list(inspect.signature(ExchangeAdapter.get_position).parameters)
    got = list(inspect.signature(cls.get_position).parameters)
    assert got == base


def test_base_declares_cancel_order():
    """契约要写在基类上，否则各适配器各写各的，迟早再次分叉。"""
    assert hasattr(ExchangeAdapter, "cancel_order")
    params = list(inspect.signature(ExchangeAdapter.cancel_order).parameters)
    assert params[:3] == ["self", "market", "order_id"]
```

- [ ] **Step 2: 运行测试确认失败**

Run: `.venv/bin/python -m pytest tests/test_adapter_contract.py -q`
Expected: FAIL，`ExtendedClient.cancel_order 签名不符合契约，实际 ['self', 'order_id']`

- [ ] **Step 3: 在基类声明契约**

`adapters/base.py` 的 `ExchangeAdapter` 加入：

```python
    async def cancel_order(self, market: str, order_id) -> None:
        """撤掉指定订单。

        market 参数对 Extended 是冗余的（订单号全局唯一），但 Lighter 撤单
        必须同时提供 market_index，因此契约统一带上它。
        """
        raise NotImplementedError
```

- [ ] **Step 4: 改 Extended 适配器**

`adapters/extended_client.py:344` 改为：

```python
    async def cancel_order(self, market: str, order_id) -> None:
        """撤单。Extended 的订单号全局唯一，market 仅为满足统一契约。"""
        del market
```

**原有函数体一行不改**，只加参数并忽略。

同时检查 `extended_client.py` 内部是否有自调用 `self.cancel_order(...)`：

Run: `grep -n "self\.cancel_order" adapters/extended_client.py`

若有，一并加上 market 参数。

- [ ] **Step 5: 改引擎调用点**

`grid/grid_engine.py:1808` 改为：

```python
            await self.ext.cancel_order(self.config.market, rec["id"])
```

**这是本任务对引擎的唯一改动。**

- [ ] **Step 6: 跑全量测试**

Run: `.venv/bin/python -m pytest tests/ -q`
Expected: PASS，不低于 505 + 4。

**既有网格测试必须全绿**——它们守着正在跑真钱的引擎。若有测试 mock 了 `cancel_order`，把 mock 签名一并改对；这属于契约变更的必要同步，不是「改测试迁就实现」。

Run: `grep -rn "cancel_order" tests/ | head -20` 先找出所有受影响处。

- [ ] **Step 7: 提交**

```bash
git add adapters/base.py adapters/extended_client.py grid/grid_engine.py \
        tests/test_adapter_contract.py
git commit -m "refactor(adapters): 统一 cancel_order 契约为 (market, order_id)"
```

---

## Task 2: 精度换算

**Files:**
- Create: `adapters/lighter_scale.py`
- Create: `tests/test_lighter_scale.py`

换算错一位就是 10 倍仓位，是本期最高风险点。

- [ ] **Step 1: 写失败测试**

创建 `tests/test_lighter_scale.py`：

```python
"""Lighter 整数换算测试。

BTC 实测精度：size_decimals=5（BTC×100000）、price_decimals=1（USD×10）。
最小下单量 0.00020 BTC —— 低于它会被交易所拒单，进而触发引擎的 reject
cooldown 反复重试，所以必须在换算层就拦下。
"""

from __future__ import annotations

from decimal import Decimal

import pytest

from adapters.lighter_scale import from_base_amount, to_base_amount, to_price


def test_btc_amount_scales_by_five_decimals():
    assert to_base_amount(Decimal("0.00317"), size_decimals=5) == 317


def test_btc_price_scales_by_one_decimal():
    assert to_price(Decimal("63400.0"), price_decimals=1) == 634000


def test_sub_precision_truncates_down():
    """向下取整。向上会让实际下单量超过引擎预期，可能突破库存上限。"""
    assert to_base_amount(Decimal("0.003179"), size_decimals=5) == 317


def test_integer_round_trip_is_lossless():
    """整数侧往返恒等。注意小数侧往返不恒等（1.5 在 decimals=0 下就回不来），
    所以这里断言的是整数往返，不是小数往返。"""
    assert to_base_amount(from_base_amount(317, 5), 5) == 317


def test_below_exchange_minimum_is_rejected():
    """0.0001 BTC 换算后是 10，非零但低于交易所最小量 0.00020（=20 单位）。
    不拦下来会被拒单，引擎进 reject cooldown 反复重试。"""
    with pytest.raises(ValueError, match="低于交易所最小下单量"):
        to_base_amount(Decimal("0.0001"), size_decimals=5, min_base_units=20)


def test_at_exchange_minimum_is_allowed():
    assert to_base_amount(Decimal("0.00020"), size_decimals=5, min_base_units=20) == 20


def test_zero_is_rejected():
    with pytest.raises(ValueError, match="必须为正"):
        to_base_amount(Decimal("0"), size_decimals=5)


def test_negative_is_rejected():
    """方向由 is_ask 表达，数量永远为正。传负数说明调用方搞错了。"""
    with pytest.raises(ValueError, match="必须为正"):
        to_base_amount(Decimal("-0.001"), size_decimals=5)


def test_float_is_rejected():
    """浮点会引入精度误差，强制传 Decimal。"""
    with pytest.raises(TypeError, match="Decimal"):
        to_base_amount(0.00317, size_decimals=5)


def test_amount_truncating_to_zero_is_rejected():
    with pytest.raises(ValueError, match="小于最小精度"):
        to_base_amount(Decimal("0.000001"), size_decimals=5)
```

- [ ] **Step 2: 运行测试确认失败**

Run: `.venv/bin/python -m pytest tests/test_lighter_scale.py -q`
Expected: FAIL，`ModuleNotFoundError: No module named 'adapters.lighter_scale'`

- [ ] **Step 3: 实现 `adapters/lighter_scale.py`**

```python
"""Lighter 下单参数的整数换算。

Lighter 的 create_order 接收整数：
    base_amount = BTC 数量 × 10**size_decimals
    price       = USD 价格 × 10**price_decimals

BTC 实测 size_decimals=5、price_decimals=1，即 0.00317 → 317、63400.0 → 634000。

换算错一位就是 10 倍仓位，因此一律用 Decimal、一律向下取整，
并对零值、负值、低于精度、低于交易所最小量的输入直接报错——
宁可下不出单，不能下错单。

取整方向统一向下截断，误差 ≤ 1 个精度单位（BTC 价格是 $0.1），
远小于网格格距（0.0986% ≈ $62），对策略无实质影响。
"""

from __future__ import annotations

from decimal import Decimal


def _check(value: Decimal, what: str) -> None:
    if not isinstance(value, Decimal):
        raise TypeError(
            f"{what}必须是 Decimal，收到 {type(value).__name__}；浮点会引入精度误差"
        )
    if value <= 0:
        raise ValueError(f"{what}必须为正，收到 {value}；方向由 is_ask 表达")


def to_base_amount(
    amount: Decimal, size_decimals: int, *, min_base_units: int | None = None
) -> int:
    """BTC 数量 → 整数。

    min_base_units 是交易所最小下单量的整数形式（BTC 为 20，即 0.00020）。
    传入后会拦下低于最小量的请求——否则交易所拒单会让引擎进 reject
    cooldown 反复重试。
    """
    _check(amount, "数量")
    scaled = int(amount * (10 ** size_decimals))
    if scaled == 0:
        raise ValueError(f"数量 {amount} 小于最小精度单位（10^-{size_decimals}）")
    if min_base_units is not None and scaled < min_base_units:
        raise ValueError(
            f"数量 {amount}（={scaled} 单位）低于交易所最小下单量 {min_base_units} 单位"
        )
    return scaled


def to_price(price: Decimal, price_decimals: int) -> int:
    """USD 价格 → 整数。"""
    _check(price, "价格")
    scaled = int(price * (10 ** price_decimals))
    if scaled == 0:
        raise ValueError(f"价格 {price} 小于最小精度单位（10^-{price_decimals}）")
    return scaled


def from_base_amount(scaled: int, size_decimals: int) -> Decimal:
    """整数 → BTC 数量。"""
    return Decimal(scaled) / (10 ** size_decimals)


def from_price(scaled: int, price_decimals: int) -> Decimal:
    """整数 → USD 价格。"""
    return Decimal(scaled) / (10 ** price_decimals)
```

- [ ] **Step 4: 运行测试确认通过**

Run: `.venv/bin/python -m pytest tests/test_lighter_scale.py -q`
Expected: PASS，10 passed

- [ ] **Step 5: 提交**

```bash
git add adapters/lighter_scale.py tests/test_lighter_scale.py
git commit -m "feat(lighter): 整数换算，含交易所最小下单量校验"
```

---

## Task 3: 订单号映射层

**这是本期最关键的设计。** Lighter 下单响应不返回订单号（`RespSendTx` 只有 `code/message/tx_hash/...`），而撤单必须传 `order_index`。因此需要：

1. 下单时自己生成 `client_order_index`
2. 事后查 `account_active_orders`，按 `client_order_index` 找到 `order_index`
3. 用它撤单

`client_order_index` **必须跨重启不重复**——进程内自增会在重启后从 1 开始，与历史订单撞号，导致映射查错单、撤错单。

**Files:**
- Create: `adapters/order_ref.py`
- Create: `tests/test_order_ref.py`

- [ ] **Step 1: 写失败测试**

创建 `tests/test_order_ref.py`：

```python
"""订单号映射测试。

Lighter 下单不返回订单号，撤单又必须要它，所以这层是撤单能否工作的前提。
撤不掉单意味着挂单在交易所端不断堆积——设计文档明确指出这种形态可能
被判定为 automation exploitation 并撤销积分。
"""

from __future__ import annotations

import json

import pytest

from adapters.order_ref import ClientOrderIndexAllocator, OrderRef, resolve_order_index


def test_order_ref_exposes_id_attribute():
    """引擎用 getattr(res, "id") 取订单号（grid_engine.py:1747），
    所以返回值必须有 .id，否则 oid=None、后续撤单必然失败。"""
    ref = OrderRef(id=12345, client_order_index=7)
    assert ref.id == 12345


def test_allocator_persists_across_restart(tmp_path):
    """进程重启后不得从 1 重新开始——会与历史订单撞号。"""
    path = tmp_path / "coi.json"
    a1 = ClientOrderIndexAllocator(path)
    first = a1.next()
    second = a1.next()
    assert second == first + 1

    a2 = ClientOrderIndexAllocator(path)   # 模拟重启
    assert a2.next() == second + 1


def test_allocator_starts_from_one_when_file_missing(tmp_path):
    assert ClientOrderIndexAllocator(tmp_path / "missing.json").next() == 1


def test_allocator_recovers_from_corrupt_file(tmp_path):
    """文件损坏时不能从 1 重来（会撞号），应报错让人处理。"""
    path = tmp_path / "coi.json"
    path.write_text("{ 坏文件", encoding="utf-8")
    with pytest.raises(ValueError, match="损坏"):
        ClientOrderIndexAllocator(path).next()


def test_resolve_finds_order_index_by_client_index():
    orders = [
        {"client_order_index": 5, "order_index": 900},
        {"client_order_index": 7, "order_index": 901},
    ]
    assert resolve_order_index(orders, client_order_index=7) == 901


def test_resolve_returns_none_when_not_found():
    """查不到可能是尚未上链、也可能是已成交，调用方决定重试还是放弃，
    这里不猜、不抛。"""
    assert resolve_order_index([], client_order_index=7) is None
```

- [ ] **Step 2: 运行测试确认失败**

Run: `.venv/bin/python -m pytest tests/test_order_ref.py -q`
Expected: FAIL，`ModuleNotFoundError: No module named 'adapters.order_ref'`

- [ ] **Step 3: 实现 `adapters/order_ref.py`**

```python
"""下单返回值与订单号映射。

Lighter 的下单响应（RespSendTx）只有 code/message/tx_hash，不含订单号，
而 cancel_order 必须传 order_index。因此下单时自己指定 client_order_index，
事后通过 account_active_orders 建立 client_order_index → order_index 的映射。

client_order_index 必须跨重启单调递增：进程内自增会在重启后从 1 开始，
与历史订单撞号，导致映射查到别人的单、撤错单。
"""

from __future__ import annotations

import json
from dataclasses import dataclass
from pathlib import Path


@dataclass(frozen=True)
class OrderRef:
    """统一的下单返回值。

    字段名 id 是为了兼容引擎的取值方式（grid_engine.py:1747 用
    getattr(res, "id")），不要改名。
    """

    id: int | None
    client_order_index: int
    status: str = ""


class ClientOrderIndexAllocator:
    """跨重启单调递增的客户端订单号分配器。"""

    def __init__(self, path: Path) -> None:
        self._path = Path(path)
        self._current: int | None = None

    def _load(self) -> int:
        if not self._path.exists():
            return 0
        try:
            data = json.loads(self._path.read_text(encoding="utf-8"))
            return int(data["last"])
        except Exception as exc:  # noqa: BLE001
            raise ValueError(
                f"客户端订单号文件 {self._path} 已损坏（{exc}）。"
                "不能从 1 重新开始——会与历史订单撞号。请人工确认后修复或删除"
            ) from exc

    def next(self) -> int:
        if self._current is None:
            self._current = self._load()
        self._current += 1
        self._path.parent.mkdir(parents=True, exist_ok=True)
        self._path.write_text(
            json.dumps({"last": self._current}), encoding="utf-8"
        )
        return self._current


def resolve_order_index(orders, client_order_index: int) -> int | None:
    """在活动订单列表里按 client_order_index 找 order_index。

    查不到返回 None：可能尚未上链，也可能已成交或已撤销。
    由调用方决定重试还是放弃——这里不猜。
    """
    for order in orders:
        got = (
            order.get("client_order_index")
            if isinstance(order, dict)
            else getattr(order, "client_order_index", None)
        )
        if got == client_order_index:
            return (
                order.get("order_index")
                if isinstance(order, dict)
                else getattr(order, "order_index", None)
            )
    return None
```

- [ ] **Step 4: 运行测试确认通过**

Run: `.venv/bin/python -m pytest tests/test_order_ref.py -q`
Expected: PASS，6 passed

- [ ] **Step 5: 提交**

```bash
git add adapters/order_ref.py tests/test_order_ref.py
git commit -m "feat(lighter): 订单号映射层，客户端订单号跨重启不重复"
```

---

## Task 4: 交易能力开关

**Files:**
- Modify: `adapters/lighter_client.py`
- Modify: `tests/test_lighter_client.py`

**⚠️ 注意现有代码里有第二个同义标志。** `adapters/lighter_client.py:24` 有类属性 `supports_trading = False`，而 `engine/hedge_engine.py:223` 与 `:249` 正在用它做分支判断。一个概念两个标志是误用温床，本任务必须把它们统一。

- [ ] **Step 1: 写失败测试**

在 `tests/test_lighter_client.py` 追加：

```python
def test_trading_disabled_by_default(monkeypatch):
    """默认只读。这是保护本金腿的最后一道闸，不能靠调用方记得传参。"""
    client = _make_client(monkeypatch, lambda r: httpx.Response(200, json={"code": 200}))
    assert client.trading_enabled is False


def test_supports_trading_follows_trading_enabled(monkeypatch):
    """supports_trading 必须与 trading_enabled 同源。

    hedge_engine.py:223/249 用 supports_trading 判断「primary 是否可交易」，
    若两个标志脱节，会在最需要动仓时输出「保持不动」。
    """
    ro = _make_client(monkeypatch, lambda r: httpx.Response(200, json={"code": 200}))
    assert ro.supports_trading is False

    rw = LighterClient(
        l1_address="0xabc", trading_enabled=True, api_private_key="0xkey"
    )
    assert rw.supports_trading is True


def test_market_order_rejected_when_read_only(monkeypatch):
    client = _make_client(monkeypatch, lambda r: httpx.Response(200, json={"code": 200}))
    with pytest.raises(PermissionError, match="只读"):
        asyncio.run(client.market_order("BTC", Side.BUY, Decimal("0.001")))


def test_place_limit_order_rejected_when_read_only(monkeypatch):
    client = _make_client(monkeypatch, lambda r: httpx.Response(200, json={"code": 200}))
    with pytest.raises(PermissionError, match="只读"):
        asyncio.run(
            client.place_limit_order("BTC", Side.BUY, Decimal("0.001"), Decimal("60000"))
        )


def test_cancel_order_rejected_when_read_only(monkeypatch):
    client = _make_client(monkeypatch, lambda r: httpx.Response(200, json={"code": 200}))
    with pytest.raises(PermissionError, match="只读"):
        asyncio.run(client.cancel_order("BTC", 1))


def test_trading_enabled_requires_key():
    """开了开关却没给密钥，必须构造时就失败，而不是下单时才炸。"""
    with pytest.raises(ValueError, match="api_private_key"):
        LighterClient(l1_address="0xabc", trading_enabled=True)
```

- [ ] **Step 2: 运行测试确认失败**

Run: `.venv/bin/python -m pytest tests/test_lighter_client.py -q`
Expected: FAIL，`AttributeError: 'LighterClient' object has no attribute 'trading_enabled'`

- [ ] **Step 3: 改构造函数**

```python
    def __init__(
        self,
        l1_address: str,
        base_url: str = BASE_URL,
        *,
        timeout: float = DEFAULT_TIMEOUT,
        trading_enabled: bool = False,
        api_private_key: str | None = None,
        api_key_index: int = 255,
        account_index: int | None = None,
        client_order_index_path: str | None = None,
    ) -> None:
        """默认只读。

        trading_enabled 是保护本金腿的显式开关：对冲机器人以默认值构造，
        因此即使配置写错也不可能让它开始交易 Lighter。
        只有做市机器人显式传 True 并提供密钥才获得下单能力。
        """
        self.l1_address = l1_address
        self.base_url = base_url.rstrip("/")
        self.account_index: int | None = account_index
        self.trading_enabled = trading_enabled
        self._api_private_key = api_private_key
        self._api_key_index = api_key_index
        self._signer = None
        self._market_meta: dict = {}
        self._http = httpx.AsyncClient(base_url=self.base_url, timeout=timeout)

        from adapters.order_ref import ClientOrderIndexAllocator
        from pathlib import Path

        self._coi = ClientOrderIndexAllocator(
            Path(client_order_index_path or "data/lighter_client_order_index.json")
        )

        if trading_enabled and not api_private_key:
            raise ValueError(
                "trading_enabled=True 时必须提供 api_private_key，"
                "否则下单时才发现缺密钥"
            )
```

- [ ] **Step 4: 把 supports_trading 改为 property**

删掉第 24 行的类属性 `supports_trading = False`，改为：

```python
    @property
    def supports_trading(self) -> bool:
        """与 trading_enabled 同源。

        hedge_engine 用这个标志判断 primary 腿是否可由引擎处置。
        两者若脱节，会在最需要动仓时输出「保持不动」。
        """
        return self.trading_enabled
```

- [ ] **Step 5: 加交易闸门**

```python
    def _require_trading(self) -> None:
        """所有会改变仓位或挂单的操作都必须先过这道闸。"""
        if not self.trading_enabled:
            raise PermissionError(
                "该 LighterClient 实例是只读的，禁止下单撤单。"
                "对冲机器人本就不应交易 Lighter；"
                "做市机器人需以 trading_enabled=True 构造"
            )
```

把现有 `market_order` 与 `close_position` 改为先调 `self._require_trading()`，并加入 `place_limit_order`、`cancel_order` 的占位（先抛 `NotImplementedError("Task 5 实现")`，但**闸门必须在前**，让 Step 1 的测试能验证到）。

- [ ] **Step 6: 跑全量测试**

Run: `.venv/bin/python -m pytest tests/ -q`
Expected: PASS

**既有对冲测试必须全绿**——它以默认（只读）方式构造，行为不应有任何变化。若有测试断言 `NotImplementedError` 而现在抛 `PermissionError`，改测试断言（语义确实变了），但**不要改回默认可交易**。

- [ ] **Step 7: 提交**

```bash
git add adapters/lighter_client.py tests/test_lighter_client.py
git commit -m "feat(lighter): 交易能力显式开关，supports_trading 与其同源"
```

---

## Task 5: 挂单、撤单、查单

**Files:**
- Modify: `adapters/lighter_client.py`、`requirements.txt`
- Modify: `tests/test_lighter_client.py`

- [ ] **Step 1: 锁版本安装依赖**

`requirements.txt` 追加：

```
# Lighter 官方交易 SDK —— 做市腿。锁 commit：签名器与返回模型可能随分支
# 变化，而单测全部绕过真实包，不锁版本等于把生产行为交给上游默认分支
lighter_sdk @ git+https://github.com/elliottech/lighter-python.git@da322db2ca4fb3fac56b463d0ade4e54960e32be
```

安装：

```bash
.venv/bin/pip install "lighter_sdk @ git+https://github.com/elliottech/lighter-python.git@da322db2ca4fb3fac56b463d0ade4e54960e32be"
```

- [ ] **Step 2: 写 SDK 契约冒烟测试**

创建 `tests/test_lighter_sdk_contract.py`。**这个文件的作用是：SDK 升级后签名若变了，测试立刻失败，而不是等实盘下单时才发现。**

```python
"""Lighter SDK 契约冒烟测试。

单测全部用假签名器，因此真实 SDK 的签名变化不会被任何测试发现——
除了这个文件。它只做一件事：断言我们依赖的方法签名和常量没变。
"""

from __future__ import annotations

import inspect

import pytest

lighter = pytest.importorskip("lighter", reason="未安装 lighter_sdk")


def test_create_order_signature():
    params = list(inspect.signature(lighter.SignerClient.create_order).parameters)
    for name in (
        "market_index", "client_order_index", "base_amount", "price",
        "is_ask", "order_type", "time_in_force", "reduce_only",
    ):
        assert name in params, f"SDK 的 create_order 缺少参数 {name}"


def test_cancel_order_signature():
    params = list(inspect.signature(lighter.SignerClient.cancel_order).parameters)
    assert "market_index" in params and "order_index" in params


def test_market_order_uses_avg_execution_price():
    params = list(inspect.signature(lighter.SignerClient.create_market_order).parameters)
    assert "avg_execution_price" in params


def test_order_constants_unchanged():
    sc = lighter.SignerClient
    assert sc.ORDER_TYPE_LIMIT == 0
    assert sc.ORDER_TIME_IN_FORCE_POST_ONLY == 2
    assert sc.ORDER_TIME_IN_FORCE_GOOD_TILL_TIME == 1


def test_send_tx_response_has_no_order_index():
    """本项目订单号映射层存在的全部理由。

    若某天 SDK 开始返回 order_index，这条测试会失败——那是好消息，
    说明可以删掉映射层。请勿直接改断言。
    """
    from lighter.models.resp_send_tx import RespSendTx

    assert "order_index" not in RespSendTx.model_fields


def test_order_model_has_fields_we_depend_on():
    from lighter.models.order import Order

    for name in (
        "order_index", "client_order_index",
        "filled_base_amount", "remaining_base_amount", "status",
    ):
        assert name in Order.model_fields
```

Run: `.venv/bin/python -m pytest tests/test_lighter_sdk_contract.py -q`
Expected: PASS，6 passed

- [ ] **Step 3: 写适配器失败测试**

在 `tests/test_lighter_client.py` 追加。**假签名器的方法签名必须与真实 SDK 一致**（真实签名见本计划「已实测确认的事实」，Task 5 Step 2 的契约测试也在守它）：

```python
class _FakeSigner:
    """假签名器。签名与真实 SDK 一致——这是本仓库反复踩过的坑。"""

    def __init__(self, err=None):
        self.calls = []
        self._err = err

    async def create_order(self, *, market_index, client_order_index, base_amount,
                           price, is_ask, order_type, time_in_force,
                           reduce_only=False, **kw):
        self.calls.append(("create_order", dict(
            market_index=market_index, client_order_index=client_order_index,
            base_amount=base_amount, price=price, is_ask=is_ask,
            order_type=order_type, time_in_force=time_in_force,
            reduce_only=reduce_only,
        )))
        if self._err:
            return None, None, self._err
        return object(), _FakeSendTx(), None

    async def cancel_order(self, *, market_index, order_index, **kw):
        self.calls.append(("cancel_order", dict(
            market_index=market_index, order_index=order_index
        )))
        if self._err:
            return None, None, self._err
        return object(), _FakeSendTx(), None


class _FakeSendTx:
    """真实 RespSendTx 没有 order_index——假对象也不能有，否则测试会掩盖
    「拿不到订单号」这个必须由映射层解决的问题。"""

    code = 200
    message = ""
    tx_hash = "0xdead"


def _trading_client(monkeypatch, tmp_path, signer=None, orders=None):
    """构造可交易客户端。HTTP 走 MockTransport，绝不触网。"""

    def handler(request: httpx.Request) -> httpx.Response:
        if "accountActiveOrders" in str(request.url):
            return httpx.Response(200, json={"code": 200, "orders": orders or []})
        return httpx.Response(200, json={"code": 200, "order_books": [
            {"symbol": "BTC", "market_id": 1,
             "supported_size_decimals": 5, "supported_price_decimals": 1,
             "min_base_amount": "0.00020"}
        ]})

    client = LighterClient(
        l1_address="0xabc",
        trading_enabled=True,
        api_private_key="0xkey",
        account_index=5626,
        client_order_index_path=str(tmp_path / "coi.json"),
    )
    client._http = httpx.AsyncClient(
        base_url=client.base_url, transport=httpx.MockTransport(handler)
    )
    client._signer = signer or _FakeSigner()
    return client


def test_limit_order_scales_to_integers(monkeypatch, tmp_path):
    """最高风险点：0.00317 → 317，63400.0 → 634000。"""
    signer = _FakeSigner()
    client = _trading_client(monkeypatch, tmp_path, signer)

    asyncio.run(client.place_limit_order(
        "BTC", Side.BUY, Decimal("0.00317"), Decimal("63400.0")
    ))

    _, kw = signer.calls[0]
    assert kw["base_amount"] == 317
    assert kw["price"] == 634000
    assert kw["market_index"] == 1


def test_buy_is_not_ask(monkeypatch, tmp_path):
    """方向搞反等于反向建仓。"""
    signer = _FakeSigner()
    client = _trading_client(monkeypatch, tmp_path, signer)
    asyncio.run(client.place_limit_order("BTC", Side.BUY, Decimal("0.001"), Decimal("60000")))
    asyncio.run(client.place_limit_order("BTC", Side.SELL, Decimal("0.001"), Decimal("60000")))
    assert signer.calls[0][1]["is_ask"] is False
    assert signer.calls[1][1]["is_ask"] is True


def test_post_only_uses_post_only_tif(monkeypatch, tmp_path):
    signer = _FakeSigner()
    client = _trading_client(monkeypatch, tmp_path, signer)
    asyncio.run(client.place_limit_order(
        "BTC", Side.BUY, Decimal("0.001"), Decimal("60000"), post_only=True
    ))
    assert signer.calls[0][1]["time_in_force"] == 2


def test_error_in_third_element_raises(monkeypatch, tmp_path):
    """SDK 把错误放在返回三元组第三个元素、不抛异常。忘记检查会让下单失败
    看起来像成功，引擎据此更新状态，造成账实不符。"""
    client = _trading_client(monkeypatch, tmp_path, _FakeSigner(err="insufficient margin"))
    with pytest.raises(RuntimeError, match="insufficient margin"):
        asyncio.run(client.place_limit_order(
            "BTC", Side.BUY, Decimal("0.001"), Decimal("60000")
        ))


def test_returned_ref_has_id_after_resolution(monkeypatch, tmp_path):
    """引擎用 getattr(res, "id") 取订单号（grid_engine.py:1747）。
    拿不到 id 就撤不掉单，挂单会在交易所端堆积。"""
    signer = _FakeSigner()
    client = _trading_client(
        monkeypatch, tmp_path, signer,
        orders=[{"client_order_index": 1, "order_index": 900}],
    )
    ref = asyncio.run(client.place_limit_order(
        "BTC", Side.BUY, Decimal("0.001"), Decimal("60000")
    ))
    assert ref.id == 900


def test_client_order_index_increments(monkeypatch, tmp_path):
    signer = _FakeSigner()
    client = _trading_client(monkeypatch, tmp_path, signer)
    asyncio.run(client.place_limit_order("BTC", Side.BUY, Decimal("0.001"), Decimal("60000")))
    asyncio.run(client.place_limit_order("BTC", Side.BUY, Decimal("0.001"), Decimal("60000")))
    first = signer.calls[0][1]["client_order_index"]
    second = signer.calls[1][1]["client_order_index"]
    assert second == first + 1


def test_cancel_passes_market_and_order_index(monkeypatch, tmp_path):
    signer = _FakeSigner()
    client = _trading_client(monkeypatch, tmp_path, signer)
    asyncio.run(client.cancel_order("BTC", 12345))
    assert signer.calls[0][1]["order_index"] == 12345
    assert signer.calls[0][1]["market_index"] == 1


def test_get_open_orders_returns_list(monkeypatch, tmp_path):
    client = _trading_client(
        monkeypatch, tmp_path,
        orders=[{"client_order_index": 1, "order_index": 900, "status": "open"}],
    )
    got = asyncio.run(client.get_open_orders("BTC"))
    assert len(got) == 1
```

- [ ] **Step 4: 实现**

在 `adapters/lighter_client.py` 实现：`_load_market_meta`（读 `/api/v1/orderBooks` 缓存 `market_id`/`size_decimals`/`price_decimals`/`min_base_amount`）、`_require_signer`（懒加载 `lighter.SignerClient`）、`_raise_tx_error`（检查三元组第三个元素）、`_auth_headers`（用 `create_auth_token_with_expiry` 生成鉴权头）、`get_open_orders`（查 `accountActiveOrders`，带鉴权）、`place_limit_order`（换算 → `create_order` → 查活动订单解析 `order_index` → 返回 `OrderRef`）、`cancel_order`、`market_order`（用 `create_market_order` + `avg_execution_price`）。

**每个方法第一行都必须是 `self._require_trading()`**（只读查询方法除外）。

- [ ] **Step 5: 跑全量测试**

Run: `.venv/bin/python -m pytest tests/ -q`
Expected: PASS

- [ ] **Step 6: 提交**

```bash
git add adapters/lighter_client.py tests/ requirements.txt
git commit -m "feat(lighter): 挂单撤单查单，订单号经映射层解析"
```

---

## Task 6: 真机验证（人工执行）

**前置条件：Task 1-5 完成且测试全绿。本任务全部由人执行，实施者不得自动进行。**

本期交付物的验收标准是「能在真实账户上安全地挂一单再撤掉」。

- [ ] **Step 1: 生成 API key**

人在 Lighter 生成 API key，填入 `.env` 的 `LIGHTER_API_PRIVATE_KEY`。

- [ ] **Step 2: 挂一张远离盘口的限价单**

写一个一次性脚本，挂**买单在现价 -20%**（绝不会成交），数量为最小下单量 0.00020 BTC（约 $12.6）。

核对：
- 交易所端能看到这张单
- 返回的 `OrderRef.id` 非空
- 价格和数量与预期一致（重点看有没有 10 倍偏差）

- [ ] **Step 3: 撤掉它**

用返回的 `id` 撤单，确认交易所端订单消失。

- [ ] **Step 4: 确认账户状态未变**

持仓仍为做市前的值，抵押品无异常变化。

**这四步走通，第一期就算交付。** 第二期（引擎解耦、做市入口、互锁、maker-first 对冲）另起计划。

---

## 不在本期范围

- 引擎解耦（5 处 `self.ext._client`，含硬止损路径那处）
- `Position.raw` 的 dict/对象兼容
- `place_position_stop_loss` / `cancel_tpsl` / `cancel_grid_orders` / `get_orders_history`
- 做市机器人入口与 `state_path` 隔离
- 对冲互锁与 maker-first 下单
- K 线行情源

这些都依赖本期的适配器地基，第二期计划会逐条覆盖。
